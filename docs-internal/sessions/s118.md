# s118 — 2026-09-05 — автономный овернайт: три карточки хендоффа закрыты

> Оператор спал. Волна: два воркера Sonnet 5 под Orca в собственных ворктри, по два круга каждый;
> координатор (Opus 5) — на замерах, фальсификации гейтов, интеграции, git и доске.
> Смержены PR **#774** (#138) и **#776** (#771); **#364** закрыта замером как «не планируется».
> Заведены **#773**, **#775**.

## Итог

| Карточка | Итог |
|---|---|
| **#364** | Закрыта `not planned` замером — прирост локали после gzip **+503 байта**, а не +47 КБ |
| **#138** | PR **#774**. `includes()` остаётся авторитетным (ADR-012), разрыв **31 файл** закрыт, два сторожа |
| **#771** | PR **#776**. 114 строк переведено, гейты `lint:i18n` + `lint:mo` в CI, оба проверены на срабатывание |

## #364 — цифра была верна, вывод из неё неверен

Карточка зафиксировала рост локали стран с ~10 КБ до ~58 КБ на блочном чекауте и просила
померить прирост **после gzip** прежде чем что-либо чинить. Замер на риге (WP 7.1 + WC 11.1.0,
250 доставочных стран), обе стороны в одном процессе со сбросом приватных кешей
`WC_Countries::$locale` / `$address_formats` между ними:

| | стран | локаль сырая | локаль gzip | `get_country_data()` сырой | gzip |
|---|---|---|---|---|---|
| без политики | 101 | 9 582 | 1 243 | 100 728 | 24 304 |
| с политикой | 251 | 57 702 | 1 989 | 143 591 | 24 807 |

**+47,0 КБ сырых = +503 байта на проводе**, около **96:1** на приросте против ~4:1 на всей
странице. Посылка проверена, а не принята: `/checkout/` отдаётся `Content-Encoding: gzip` на
голом Apache из `wp-env`, без единой настройки. Это внутри «пары КБ», названной самой карточкой
условием закрытия, поэтому подпункты 2 и 3 не делались — они оптимизируют тот самый сырой объём,
который на проводе стоит полкилобайта.

Общий урок вынесен в готчу `a-raw-payload-size-is-not-a-wire-cost-measure-it-after-gzip`: сырой
счёт байт **читается как законченный замер** — у него есть единица, он воспроизводится, он едет
прямо на карточку, и ничто в нём не сообщает, что для заданного вопроса он промахивается на два
порядка.

## #138 — разрыв в 31 файл, и сторож не тот, который казался нужным

Карточка оценивала расхождение в «шесть классов §8 и все классы SP-5». Замер: из 71 настоящего
класс-файла под `woodev/shipping-method/**` в `includes()` было прописано **39**. Разрыв — **31**.
Class-map при этом был не виноват: перегенерация дала байт-идентичный файл.

Выбран вариант B (`includes()` авторитетен) — **не потому, что нашлась опасность порядка
загрузки**, её не нашли, а потому что доказать безопасность на реальной vendored-загрузке нечем,
а этот класс ошибки уже четырежды давал фатал.

**Главное открытие круга — какая сюита что ловит.** Замерено инъекцией, а не выведено:

| отказ | unit без новых тестов | unit с ними | интеграция |
|---|---|---|---|
| висячий `require_once` (файл удалён) | **ловится** — 3 ошибки через `YandexPilotFixtureTest` → `Framework_Resolver` | ловится | **ловится** — падение в бутстрапе |
| запись class-map, которую не требуют | не ловится | **ловится — единственным гейтом** | **НЕ ловится**: `OK (143 tests, 530 assertions)` |

Отсюда два вывода, и оба обратны интуитивному. Висячий `require_once` **никогда не был невидим** —
`test_every_required_shipping_file_exists()` даёт читаемое сообщение вместо фатала в бутстрапе,
это польза, но скромная, и так и записано. А обратный сторож **действительно единственный**:
интеграция полностью зелёная с классом, выпавшим из `includes()`, потому что рантайм-автозагрузчик
достаёт его из `class-map.php`. Ровно так список и сгнил до 31 файла, никем не замеченный.

Первая редакция ADR-012 утверждала обратное («зелёный unit не доказывает ничего, только
интеграция»); координатор заменил это утверждение матрицей.

## #771 — перевод и, важнее, принуждение

150 непереведённых английских msgid (не 147: воркер нашёл 3 записи с формами множественного
числа, которые скрипт карточки пропустил). Классификация **по пути отрисовки**: 114 переведено,
3 в allowlist как никем не читаемые (исключение, аргумент `ltrim()`, `_doing_it_wrong()`), **33**
— как недостижимые, потому что их исходных файлов в дереве нет (`class-warehouse-admin.php`,
`abstract-warehouses-controller.php` — проверено). На уборку заведена **#775**, заморожена за
**#567**.

Правило «английская админская строка должна быть переведена» было записано в `AGENTS.md` и
**ничем не принуждалось** — поэтому и накопилось. Теперь `lint:i18n` и `lint:mo` в `ci.yml`.
`lint:mo` — чистый ридер, Docker в CI не нужен.

**Первый круг привёз рукописный компилятор `.po` → `.mo`.** Файл рабочий, но **другой**: 61 421
байт против 61 491 у `wp i18n make-mo`. Это ломает записанный инвариант из готчи
`the-mo-is-reproducible-from-the-po`, по которому будущие сессии определяют дрейф каталога.
Компилятор снят, `.mo` пересобран штатным инструментом, md5 сошлись.

## Проверки координатора, которые что-то поймали

- **Матрица гейтов #138** — опровергла собственное утверждение ADR и мою же формулировку
  первого круга (см. ниже).
- **`.mo` против `wp i18n make-mo`** — поймала подмену артефакта, которую все гейты пропускали:
  оба `lint:*` были зелёные на рукописном компиляторе.
- **`.po` ничего не потерял**: msgid 753 → 753, msgid_plural 14 → 14, obsolete `#~` 195 → 195,
  obsolete msgid 92 → 92, пустых `msgstr` 446 → 335. Это ловушка из
  `a-po-merge-that-drops-obsolete-entries-still-looks-well-formed`.
- **Плейсхолдеры**: 443 переведённые строки, **0** расхождений по составу `%s`/`%1$s`.
- **Запрещённая лексика**: «чекаут»/«фреймворк» в добавленных переводах — 0.
- **Оба новых гейта фальсифицированы**, а не только прогнаны на зелёном.

## Две мои ошибки

1. **Я переоценил находку по #138 и отправил её воркеру как факт.** Написал «TEST STAYS GREEN,
   висячий `require_once` НЕ ловится» — верно про тот тест, неверно про сюиту: обе её половины
   на нём падают. Проверил я это только следующим шагом. Воркер успел отработать по неверной
   посылке раньше, чем доехала поправка, и ADR получил формулировку, которую пришлось чинить
   отдельным коммитом. Урок ровно тот, что уже записан у меня в памяти: **правдоподобный вывод,
   записанный как факт, закрывает вопрос** — на брифе воркеру планка та же, что на карточке.
2. **Я дал воркеру A неверную инструкцию** («напиши компилятор `.mo`, инструмента нет»), не
   сверившись с готчами до брифа: `wp i18n make-mo` есть в контейнере рига и это записанный
   инвариант. Поправка ушла двумя письмами и **не была прочитана** — см. ниже.

## Ловушка Orca, стоившая круга

**`orchestration send --to dispatch:<id>` — почта, а не инъекция в промпт.** Воркер читает её
только когда сам зовёт `orchestration check`. Ни один из двух воркеров Sonnet за всю работу его
не позвал, поэтому обе мои поправки (не писать компилятор `.mo`; готовая команда с путём внутри
контейнера) пролежали непрочитанными до конца круга, и воркер A построил ровно то, что первое
письмо запрещало.

Что делать: **требовать `orchestration check` в самом брифе, явным пунктом**, и после отправки
поправки убеждаться по буферу терминала, что она прочитана, — либо слать `terminal send`, если
дело срочное. Готча: `orchestration-mail-is-not-read-unless-the-worker-calls-check`.

## Мелочи, проверенные попутно

- **Ворктри Orca ВИДНЫ внутри контейнеров рига** (`.wp-env.json` мапит корень репозитория, а
  ворктри лежат в `.orca/` внутри него) — так `wp i18n make-mo` компилировал `.po` воркера на
  месте. Но **интеграцию оттуда гонять нельзя**: резолвинг фикстуры упирается в
  `WOODEV_FRAMEWORK_DIR`, указывающий на основной чекаут. Гонять через detached-checkout
  основного дерева.
- **Гейты перемерены целиком** на `ae5fd31`: unit **3522**/8645/**1**, интеграция **143/530**,
  jest **1628**/25, phpcs чисто, phpstan level 3 чисто, `lint:docs` OK. **`CURRENT-STATE` врал про
  интеграцию** (138/522) — хендофф s117 был прав, файл поправлен.
- **e2e перепрогнан: 7/7** на новом стеке WP 7.1 + WC 11.1.0 (в s117 не гонялся — единственный
  гейт, остававшийся старее рига).
- **Доска чиста**: 61 открытая карточка, все на доске №6, у каждой «Приоритет», Инбокс пуст.
- Пустой каталог-остаток `.orca/worktrees/woodev_framework/s87-critic-472r3/` от s87 — не
  зарегистрированный git-ворктри, удалён вместе с ворктри сессии.

## Гейты

| | на `ae5fd31` (база) | #138 `2fadd19` | #771 `fa33b42` |
|---|---|---|---|
| unit | 3522 / 8645 / 1 | **3524 / 8647 / 1** | 3522 / 8645 / 1 |
| интеграция | 143 / 530 | **143 / 530** | **143 / 530** |
| jest | 1628 / 25 | 1628 / 25 | 1628 / 25 |
| phpcs · phpstan L3 | чисто | чисто | чисто |
| e2e | 7 / 7 | — | — |
| `lint:i18n` · `lint:mo` | — | — | OK (752/36 · 417+заголовок) |

## Вторая половина — его проверка на риге, и два моих промаха

Оператор пошёл смотреть #771 и не увидел ничего. **Обе причины мои, обе — нарушение одного
записанного правила** (сверить подпись И достижимость до того, как отправлять его на риг):

1. **Риг работает на `en_US`.** `is_textdomain_loaded: NO`, WordPress ищет
   `woodev-plugin-framework-en_US.mo`, которого нет и быть не должно. **Русский каталог там не
   грузится вообще** — ни одна из 114 строк. Каталог при этом верный: прочитал `.mo` напрямую,
   минуя локаль, — 61 491 байт, 417 записей, «Packing algorithm» → «Алгоритм упаковки».
   ⚠ И это же объясняет, почему ДЕФЕКТ был виден, а ФИКС не будет: ~300 записей с русскими msgid
   рендерятся русскими при любой локали, английские — английскими. Риг умеет показать проблему i18n
   и не умеет показать её решение. `WPLANG` не переключается: WordPress отбрасывает `ru_RU`, потому
   что языков ядра нет, а контейнер не достаёт до w.org.
2. **«Алгоритм упаковки» недостижим.** Поле под `supports_box_packing()`, и замер по всем трём
   методам рига даёт `no` — `FEATURE_BOX_PACKING` не объявляет ни одна фикстура. Я взял пример из
   карточки как есть и не проверил ни того, ни другого.

Готча: `the-rig-runs-en-us-so-no-translation-ever-renders-there`.

## Его второй вопрос дал решение, которого не было

«Что это за страница `tab=integration&section=woodev_test_shipping_method`?» Факты: это ФИКСТУРА
(`Woodev_Test_Cdek_Integration`, ключи тестового контура СДЭК), заведена `bf86ece` (#403), последний
раз тронута `cda064b` (#647, s104); в s118 `tests/_fixtures/` не менялся вовсе. Фреймворк вкладку не
навязывает: `get_integration_handler()` в базе `null`.

Но под вопросом лежал настоящий, нигде не записанный: **должен ли плагин v2 вообще класть настройки
на вкладку WooCommerce, когда есть `Woodev → Настройки`.** Это тот же класс ошибки, что утопил s116.
Завёл **#777** в Инбокс с тремя вариантами; **оператор ответил в тот же день**: по умолчанию —
`Woodev → Настройки`, но от вкладки «Интеграции» не отказываемся, используем при необходимости.

Записано как **Rule 8** в `AGENT-RULES.md` с его прямой цитатой, обоими швами
(`Woodev_Plugin::get_settings_providers()` и `Shipping_Plugin::get_integration_handler()`),
предупреждением про контракт хранилища `woocommerce_{plugin_id}_settings` — и **явным запретом
выдумывать за него границу «при необходимости»**: он ответил про дефолт, а не про границу.
Отдельно записано, что фикстуру рига приводить к дефолту НЕ надо — она единственная живая
демонстрация механизма-исключения. #777 закрыта.

⚠ **Оба док-гейта теперь переполнены**: `CURRENT-STATE.md` и хендофф держатся с запасом в единицы
байт, и в этой сессии я трижды подряд шлифовал вместо выноса. Следующее добавление требует выноса
раздела в вики, а не сжатия.

## Его решение по #771: не проверять сейчас

Я предложил поднять на риге `ru_RU` mu-плагином, чтобы он посмотрел 114 строк. **Оператор
остановил:** «пока не будем на этом останавливаться. У нас всё равно есть пред-релизная задача
обновить и проверить все переводы. Тогда этим и займёмся. Сейчас просто тратим время впустую.»

Правильно, и урок мой: у проверки уже было плановое место — **#567** (майлстоун `v2.0 релиз`,
именно про язык msgid и каталог), а **#775** и так заморожена за ней. Я собирался строить разовую
оснастку (mu-плагин с подменой локали, фикстуру с `FEATURE_BOX_PACKING`) ради прохода, который
через одну карточку делается заодно и целиком. Обязательство перенесено комментарием НА #567 —
условия прохода, инверсия с русскими msgid и недостижимость «Алгоритма упаковки» записаны там,
чтобы не восстанавливать это заново.

**Обобщение, стоящее того, чтобы его помнить:** прежде чем строить оснастку под проверку, посмотреть,
нет ли карточки, которая эту проверку уже несёт. Разовая оснастка под чужую плановую работу — это
и есть «тратим время впустую».

## Related

- [../GOTCHAS.md](../GOTCHAS.md) — готч **279**
- [../adr/012-shipping-includes-stays-authoritative.md](../adr/012-shipping-includes-stays-authoritative.md)
- [s117.md](s117.md) — предыдущая сессия
