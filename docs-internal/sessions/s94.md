# s94 — 26.08.2026

**#530 merged, the #536 stack rebuilt after GitHub closed it, and three client-side location
defects fixed and rig-verified: #541, #539 (part 1), #540.**

## What landed

| | |
|---|---|
| Merged to `main` | **#535** (`6017a0c`) — #530, popular settlements shown to the customer |
| Open PR | **#543** — #536, #538, #541, #540, part 1 of #539. All 19 checks green. **Waits on the operator's rig pass.** |
| Gates on the branch | `composer check` **2800** / 6839 / **66 skipped**; jest **1539** |
| Gotchas | 208 → **209** |

## The stacked-PR mechanic, hit with the gotcha already written for it

Squash-merging #535 with `--delete-branch` removed the branch that was #537's BASE. GitHub closed
#537 on the spot, and neither recovery works:

```
gh pr reopen 537  → Could not open the pull request.
gh pr edit 537 --base main → Cannot change the base branch of a closed pull request.
```

Retargeting needs the PR open; reopening needs the base branch to exist. **A closed PR whose base
branch is gone is permanently closed.**

Recovery took one command — `git rebase --onto origin/main d966b00` — dropping the two #530 commits
the squash had absorbed, verified by the diff-stat against `main`, then a new PR (#543) carrying the
old body plus a comment on #537 pointing at it.

**This was already in `gotchas/stacked-pr-github-mechanics.md`, and I did not read it first.** The
session-start protocol says to scan the `[topic/*]` tags relevant to the task; merging a stacked PR
is squarely `[tooling/git-merge]`. The gotcha now carries the recovery path and the cheap
prevention (merge the base WITHOUT `--delete-branch`, retarget the downstream while it is still
open, delete the branch last).

## #541 — the cause was an asymmetry, not the queue

The operator had already named the reference: *«этот сценарий уже работает, если у региона тип
„Список с поиском“»*. Following that literally is what found it.

Under `ajax-select2` the record IS the pick — `resolveAndSelect()` looks it up in its own
`dataByKey` and calls `options.onSelect()` synchronously, so every piece of s90's busy/lock
machinery runs in the same tick as the click. Under `related-list` the region `<select>` is
WooCommerce's own and carries nothing but label text, so the renderer must match that text against
`GET /location/list` — **9.7 s cold on the rig** — before it has a record to hand over at all. For
all of that time nothing had been told a pick happened.

The seam is therefore a pick announced by **level**, not by record: `options.onResolving()` returns
a `release()` for the case where the identity never arrives. Optional exactly as `onAbandon` is;
`ajax-select2` neither needs nor calls it.

Descendant locking needed no new mechanism — `refreshDependentLocks()` already locks every level
deeper than the busy one. **That is why the marker must not be `queued`:** a queued marker means
"the request has not left, it confirms nothing" and `hasUnconfirmedParent()` ignores it on purpose,
whereas this one asserts what is true — the level's identity is unknown. Raising it as `queued`
shows the spinner and silently drops the lock, i.e. exactly the half of #541 the operator saw.

### Measured, cold path, interception installed before page load

```
>>> click on the region      +0 ms
→ location/list              +4 ms
SPINNER on shipping_state   +10 ms      ← was +10 558 ms
LOCK shipping_city          +10 ms
← location/list           +9 724 ms     ← genuinely cold
← location/select        +15 932 ms  → spinner off, field unlocked, list = СПб's three
```

**Control**, same branch with the fix reverted (`servedHasFix: false` asserted before drawing any
conclusion), at the operator's own +800 ms mark: no spinner, `shipping_city` `aria-disabled` =
`null`, host not busy. Honest caveat recorded on the card: the control's `/list` returned faster
(server warmed by earlier runs), so its exposure window was SHORTER than the fixed run's — the
comparison is conservative, not flattering.

## #539 part 1 — and a frame that would have been worse than the defect

The rig measurement **corrected the card**: select2 does not replace the list while a query runs.
`showLoading()` PREPENDS its message and leaves the previous results below it, so the field went on
offering «Санкт-Петербург» and «Репино» for 9.6 s to someone who had typed «Пушк».

Narrowing the popular list locally fixes that. The near-miss: a term matching NO popular entry
narrows to `[]`, and select2 asks `language.noResults` for that render — so with the in-flight flag
raised after the narrowing, «Выборг» rendered «Поиск не дал результатов» **instantly, over a search
that had not been sent**. Same conflation #405 exists to prevent. → gotcha
`an-empty-list-while-the-search-runs-is-not-a-zero-result`.

**The test that could not see it.** The first version sampled `language.noResults` after
`transport()` returned, by which point the flag is `true` in both orderings — the probe stayed
GREEN across all 149 tests while the rig showed the defect plainly. select2 asks the hook DURING
the render (`selectWoo.full.js:3586-3600`), so the harness now asks it from inside `success`. Then
the probe goes red. Third time on this branch that a green suite hid a real defect.

The double `success()` was read out of the rig's own vendored `selectWoo.full.js` before being
relied on, not assumed.

### Round 2 — the operator's pass found the half the fix had taken away

With a local match the field showed «Пушкин» and nothing else: no sign a search was still running.
The zero-match case looked right only by accident, through the `noResults` wrap.

Read in the vendored bundle rather than guessed: `container.on('query')` PREPENDS the loading row
(`showLoading()`, `selectWoo.full.js:940-954`) and `Results.prototype.append()` opens with
`hideLoading()` (:856). **Every `success()` strips the indicator** — the payload is irrelevant,
painting at all removes it. The early paint was always going to take it; only the empty branch
re-stated it, by a different route.

The early paint now carries its own row, shaped as `showLoading()` shapes its own —
`{disabled: true, loading: true, text}`. `disabled` is load-bearing: `option()` (:960-975) drops
`data-selected`, and both the click binding (:1232) and `highlightFirstItem()` (:891-905) filter on
that attribute, so the row is unclickable, unselectable by keyboard and never takes the first-item
highlight. Confirmed in the live DOM. Placed last; omitted when there is no localized
`i18n_searching`.

This unified the branches — a zero-match narrowing is a one-row list, so `noResults` is no longer
reached there. The `searchInFlight` guard is kept anyway as belt-and-braces.

```
«Пушк»   +406 ms  Пушкин · Searching…        → +6216 ms Пушкин, Санкт-Петербург, Россия
«Выборг» +416 ms  Searching…                 → +6048 ms Поиск не дал результатов
```

**The lesson worth keeping:** the first fix was verified against the case the card described
(«Пушк» loses the stale rows) and not against what the whole frame then looked like. The operator
looked at the frame.

## #540 — a new msgid, but only after checking

Every `wc_country_select_params` key and every key in this layer's own `i18n` block was checked
first; none means "start typing a name". #526's rule has its own escape hatch — a string with no
ready-made source goes in `Checkout_Config` — so that is where it went, filterable through
`woodev_location_i18n`, and pinned distinct from `placeholder` («Выберите…») by a unit test.

## A doc claim that was wrong, caught by measuring

The #528 tag row did not appear for a zero-result term. Before reporting a regression I read the
option: `woodev_location_allow_custom_settlement` = **`no`**. The gate was doing its job. **The s93
handoff records that option as `yes`; it is not.** CURRENT-STATE now carries the measured values.

Also caught by the docs gate: I "corrected" the gotcha count from 208 to 210 by counting
`gotchas/*.md`, which includes `README.md`. 208 was right; 209 is right now.

## Probes

Fourteen red probes across five commits, each reverted. The two that earned their keep:

- **#541, announce inside the `.then`** (the pre-fix shape) — caught by the ordering assertion
  ALONE, while the other two tests stayed green.
- **#539, in-flight flag after the narrowing** — stayed green against the first version of the
  tests. The test was wrong, not the probe.

## Related

- [CURRENT-STATE.md](../CURRENT-STATE.md)
- [gotchas/an-empty-list-while-the-search-runs-is-not-a-zero-result.md](../gotchas/an-empty-list-while-the-search-runs-is-not-a-zero-result.md)
- [gotchas/stacked-pr-github-mechanics.md](../gotchas/stacked-pr-github-mechanics.md) — Symptom 5
- [next-session-prompt.md](../next-session-prompt.md)
