# s93 — 2026-08-25

**Merged: #526** (PR #533, `9ca8e78`). **PRs #535 (#530) and #537 (#536 + #538 + #541) open — both
awaiting the operator** (customer-facing UI, not self-merged; #537 stacked on #535 so one rig pass
covers both). He ran that pass and it produced four more cards.

Orchestration shape: I implemented #526 directly (small, single-file, fully decided by the operator
on the card), Codex critiqued it twice, and #530 — the substantial piece — went to a Sonnet worker
in its own worktree.

## #526 — select2's UI strings now come from `wc_country_select_params`

The reported symptom was «No results found» in English on a Russian checkout, in the
`ajax-select2` settlement mode. The cause: `config.language` was only ever wired on the **non-ajax**
branch of `selectConfigFor()`, and only when `seed.onAbandon` happened to be a function.

**The reported string was not the worst of it.** `minimumInputLengthFor()` floors the settlement
field at 2, so `inputTooShort` — «Please enter 2 or more characters» — is what the customer stares
at *before typing anything*, on every visit. More constant than the defect that got reported, and
nobody had noticed it.

The operator's ruling on the card set the approach: do not invent translations, take WooCommerce's
own. `select2LanguageFor()` sources all seven messages from `wc_country_select_params`, copied
key-for-key from WooCommerce's `assets/js/frontend/country-select.js` **read in the rig container**,
not recalled. `Checkout_Handler` declares `wc-country-select` as a dependency, which is what makes
the global both present and printed first.

Three judgement calls, all deliberate:

1. **Wired for every strategy**, not just ajax — a `related-list` region field carrying no
   `onAbandon` hit the same untranslated defaults.
2. **`errorLoading` returns `i18n_searching`, not `i18n_ajax_error`.** Looks like a bug in the
   reference; is not. WooCommerce's documented workaround for select2/select2#4355. Pinned by a
   test so a future reader does not "fix" it.
3. **`noResults` prefers our own `emptyText`** over WooCommerce's generic `i18n_no_matches` —
   otherwise the two settlement modes would disagree about the same outcome.

## The critic earned its keep — on the one claim I flagged as weakest

The brief told Codex which claim I least trusted (C5: "returning `undefined` makes select2 fall
back to its own English"). It refuted exactly that one, and it was right.

`selectWoo.full.js:2236,4934-4940` — `customTranslation.extend( baseTranslation )` is
`$.extend( {}, EN, ours )`. **Ours wins for every key we define.** So a defined callback shadows
English permanently, and returning `undefined` renders a *blank* message box — strictly worse than
the English it was meant to avoid. The fix is to **omit** the key.

I re-measured the refutation against the selectWoo source before acting on it rather than taking
the critic's word, which is the standing rule; it held.

New gotcha: `a-select2-language-callback-that-returns-undefined-renders-blank`.

**A re-critic on the repair then refuted three more things — all of them comments, none of them
behaviour.** The blank-message corner was called unreachable (it needs two public filters abused at
once — rare, not impossible); "both plural branches are reachable" was claimed for all three plural
keys when only `inputTooShort` is live here; and the test comment blamed a dequeue route the critic
could not demonstrate, when the real one is `woocommerce_get_script_data`. Fixed as comment-only.

**That is now four times on this one file that an inference was recorded in a docblock as a fact.**
Each time it closed the question for the next reader. The countermeasure that actually worked here
was naming the weakest claim in the critic's brief and telling it to attack that specifically.

## Two stale numbers, and why they matter

The s92 handoff carried `composer check` 2783/6800/66 and jest 1459. Both wrong. Measured by
`git stash`-ing my diff and re-running on a clean `main`: **2787/6813/66 and jest 1467**. My change
added 7 jest tests and no PHP tests, which reconciles 1467 → 1474 exactly.

Had I trusted the handoff, the 1459 → 1474 gap would have read as "+15 tests from a 7-test change"
and I would have gone hunting, or worse, shrugged. **A gate number copied forward from a previous
handoff is an INFERENCE.** Re-measure before comparing.

## Rig state drift

`woodev_location_allow_custom_settlement` is **`yes`** on the rig; CURRENT-STATE recorded it as
deleted (off). The operator turned it on after s92 closed. It matters for measurement: with it on,
the #528 tag row occupies the zero-result slot, so the «ничего не найдено» string never renders.
Any empty-state measurement has to flip it to `no` and restore it afterwards. Done and restored.

## Verification that actually proved something

The canary test had a **control**, which is what made it evidence rather than a coincidence:
a canary written into `i18n_input_too_short_n` rendered as «CANARY-526 введите ещё 2» with `%qty%`
substituted, while the **un-canaried** `i18n_input_too_short_1` kept WooCommerce's real value.

The zero-result render was asserted on the **rendered text**, not a callback return value —
«Поиск не дал результатов. Попробуйте изменить запрос.» That distinction is a standing correction
from earlier sessions and it was needed here: the callback returned the right string while the
rendered dropdown still showed a tag row, because the option was on.

Four red probes on the first version, two on the repair, every one going red.

## Orchestration notes

- `worker-start --agent codex` failed twice at `dispatch_input` with `agent_prompt_blocked` — the
  update dialog. Two failures circuit-broke the task to **`failed`** (not `blocked`, which is what
  the existing gotcha's recovery section describes), and `dispatch --inject` then refuses it until
  `task-update --status ready`.
- **`terminal wait --for tui-idle` returned `satisfied: true` with the update dialog still on
  screen.** The existing gotcha says it reports `satisfied:false` until ESC. It did not. Only
  reading the screen and grepping it is reliable — and Enter on that dialog means "Update now",
  which loses the injected brief.
- The hand-made Codex terminal took a **second** `dispatch --inject` for the re-critic with no
  re-launch and no second ESC.

Both recorded into `starting-codex-under-orca-needs-four-steps-not-one`.

## #530 — the popular list finally reaches the customer

#488 shipped storage, clocks, lazy verification and merchant tools (D1–D8). The spec's own promise
— *"something useful before the customer types"* — never became a D-decision, so it was never built.
Nothing read the store on the checkout, which is why the operator could not exercise it at all.

**Design fork settled by measurement, before any code:** a config block, not a REST route.
`minimumInputLengthFor('settlement') === 2` means the empty state must exist at ATTACH time, `/suggest`
answers in 6–10 s so a round-trip there is unaffordable, and the list is capped at 30 rows.

Delegated to a Sonnet worker in its own worktree. **It corrected my brief's central assumption** —
`Location_Record::region()` carries only `{name, type}`, not a key; `ancestors()` is the real
mechanism, matching `is_within()`. Licensing the worker to contradict the brief keeps paying.

### Round 1 passed every gate and was broken twice over

Both defects appeared only for a **fresh customer in an isolated browser context** — the state the
feature exists for, and the one neither of us had tested.

1. **The field arrived pre-selected to the most popular city.** `selectedIndex 1`, chip reading
   «Санкт-Петербург» for someone who had never touched it, and that value posts. The class of bug
   #350 and #502 exist for. Cause, found in real Chrome rather than jsdom: the popular `<option>`s
   were appended with none carrying `.selected`, so the browser's own reset-selectedness algorithm
   took the first, and the blank option arrived too late to win.
2. **The popular list was never rendered.** Options in the DOM, dropdown saying «Please enter 2 or
   more characters». **Seeding real `<option>`s is not sufficient in ajax mode** — that was wrong in
   the card's own warning and wrong in my brief. `related-list` renders DOM options because it is a
   LOCAL select2; the ajax strategy uses its own data adapter and enforces the floor. Fixed by
   scoping `minimumInputLength` to 0 for a level carrying a popular list and answering the empty
   term locally, with no network call.

Round 2 fixed both, verified live: `selectedIndex 0`, chip «Выберите…», and six popular cities
listed with nothing typed.

## The worst mistake of the session was mine, and it was a method mistake

Checking region filtering, I changed the region, waited **3000 ms**, read the dropdown, saw all six
entries, and filed **#534** calling region filtering structurally impossible — with a tidy
explanation from WooCommerce state codes and `ancestors` that convincingly accounted for it.

The round-trip on this rig is **6–10 s**. I had read the list before the change landed. Re-measured
with a control: Москва → Внуково/Москва/Бутово, Санкт-Петербург → СПб/Пушкин/Репино. It works.
#534 closed as `not planned`, PR description rewritten.

**s92's handoff carried this exact warning** («ждать по факту появления строки, а не по таймеру»).
Knowing it did not prevent it. What prevents it is method: poll for the expected state, and always
flip to a second value as a **control** — an unfiltered list and a not-yet-arrived list are the same
bytes, so one region proves nothing. New gotcha:
`a-rig-measurement-on-a-timer-invents-a-defect-that-is-not-there`.

The through-line of the whole session: **a confident, well-explained claim is the dangerous kind.**
It happened in a docblock (#526's C5), in three comments (the re-critic), and in a card of mine
(#534). Each time the explanation was elegant and the conclusion was wrong.

## Budget

The round-2 worker terminal reported **80% of the weekly limit used** (resets 08:00 Europe/Moscow).
I dropped the planned Codex critic on #530 rather than spend into that ceiling — the two rig-driven
rounds had already found more than a static review would have.

## #536 — the default locality nobody could see

The operator asked mid-session whether «город по умолчанию» worked. It did not, and it never had.
Measured on clean `main`: the server resolved «Москва» and rates were calculated from it, while the
customer got three empty fields and a locked address. **Not a regression** — the half-fix from
#502/#509 closed "an implicit default must not unlock the address" and left "the default is
invisible" without a card.

**I then made the session's worst mistake.** I found D11 in the spec, which said implicit records
"are not treated as the customer's answer", and produced it as an argument against his own fresh
decision. His correction: *«спеку то не я пишу а ты… ты в какой-то из сессий записал не так,
возможно додумал, неверно трактовал. А сейчас дошло до реализации и делаешь по спеке всё чётко»*.

He is right, and it generalizes: **a spec D-decision is the agent's TRANSCRIPT of him, never
authority over him.** Where the two disagree he wins and the spec gets corrected. Recorded in the
spec itself, above the amendment, so the next reader cannot repeat it.

Decision implemented: `fixed` is shown exactly as if the customer picked it; `geoip` stays
invisible. The distinction that was missing is that `implicit` covered two states —
*implicit-because-we-guessed* and *implicit-because-the-merchant-configured-it*.

Round 1 passed 2798 tests and did not work. The blocker: the checkout config sent only `{key}` for
the chain, so the client had **no text to write**. Round 2 killed my own hypothesis — the region
field is a plain `<input>` at `prefill()` time and the write succeeds; WooCommerce's
`country-select.js` later promotes it to a `<select>`, its value-based restore fails on the
UPPERCASE convention, and the resulting empty `change` makes `clearDescendants()` wipe the
already-correct settlement. Fixed by deferring the write to `country_to_state_changed`.

## Process: three things changed, all of them because he pushed

**1. The handoff got a schema and a gate.** His complaint: it is the only document read first every
session and the only one with no rules, so an unfinished commitment left the project by simply not
being mentioned again. `DOCS-SCHEMA.md` now fixes seven sections; the load-bearing check is the
**diff against the previous handoff** — every `#N` in the old carry-over must still be accounted
for. Red-probed both ways. Built in the `woodev-docs-system` skill FIRST, then projected here, at
his instruction.

**2. The handoff is untracked — the repo is public.** Then measured and reported: 433
`docs-internal` files are already public, so hiding one file is theatre, and git history is not
fixed by a `.gitignore`. He concluded the repo will probably go private instead. **Not done — it
takes down GitHub Pages unless the account is on Pro, and that is his call.**

**3. `DECISIONS.md` was proposed and rejected** — one more file to read, right after a docs
cleanup. The handoff schema is the better answer to the same problem.

## The orchestration incident

The #536 round-2 worker edited MY primary checkout instead of its worktree and left my HEAD
detached. Nothing was lost (only debug `console.log` probes leaked, removed), but the cause was
mine: I told it to "reproduce on the rig", and **the rig serves the primary checkout** — from a
worktree it physically could not. New standing rule, applied mid-round: **the rig is the
coordinator's**; a worker describes the scenario and the coordinator measures.

## Gates

`main`: **2787** / 6813 / **66 skipped**, jest **1474**. PR #535: **2791** / **66**, jest **1491**.
PR #537: **2798** / 6835 / **66**, jest **1502** — all measured in the PRIMARY checkout.

## The operator's rig pass — four cards, and one of them broke my model twice

He accepted #530's list and #536's auto-fill, then reported four things. Two were defects in what
he was reviewing and were fixed in the same branch; two went to the backlog.

**#538 — the auto-filled region did not scope the popular list.** Measured: the region field read
«МОСКВА» while the list still offered all six entries, three of them in Saint Petersburg.
`scopeKeyFor()` answers "what key did the PARENT level record", and the #536 path records no region
key. **My proposed fix was wrong and the card carries the correction**: I wanted the server to name
the ancestor's level, and `Location_Record::parse_ancestors()` refuses a `level => key` map
DELIBERATELY — measured against a live DaData capture where one row carries `city_fias_id` and
`settlement_fias_id` at once. The filter now asks the one question the design says it can answer:
do these two localities share an ancestor. Verified live — the list under «Москва» is now exactly
Внуково/Москва/Бутово.

**#541 — the spinner lagged the customer, and I diagnosed it wrong twice.**

Round 1: he said "settlement is instant, region takes ~5 s". I measured `/select` DURATION and
found both at ~14 s, and answered that there is no difference. Technically right, entirely beside
the point — he was describing what he could SEE.

Round 2: he clarified it was the spinner. I measured a queued pick sitting behind an in-flight
`/select`, moved the marker from send to enqueue, and shipped it. That fix is real (and it exposed
that one flag was doing two jobs — raising it on enqueue broke s90's own regression test, because
"show a spinner" and "a parent is unconfirmed" are different claims). **But it did not fix his
case.**

Round 3: he re-checked and it still lagged. He also questioned my repro instructions, which is what
finally cracked it — I had been fitting his report to my model instead of reproducing his sequence
literally. With the fetch hook installed BEFORE page load:

    >>> click on the region     +0 ms
    → location/list             +2 ms
    ← location/list        +10 508 ms
    → location/select      +10 532 ms
    SPINNER                +10 558 ms

The `/select` queue was never involved. In `related-list` a region pick first fetches `/list`, and
`onSelectFor()` runs only when that returns, because the renderer takes the region's record from
that same response. The gap is BEFORE the queue.

**I had twice measured the spinner at 47 ms and concluded it was fine** — both runs were on a
settled page where the region list was cached and `/list` returned instantly. He was hitting the
cold path. That is the same trap as #534 earlier the same day: a measurement whose state I had not
controlled, producing a confident wrong answer.

He then specified the behaviour he wants, and named a reference: **the scenario already works when
the region is `ajax-select2`.** So the fix is to bring `related-list` to what our own code already
does, not to invent one. Not started — the session ended here on context.

## What the operator changed about how we work

Three process corrections, all his, all recorded durably rather than left in chat:

1. **A spec D-decision is my transcript of him, not authority over him.** I produced D11 against his
   own live answer; he pointed out he does not write these specs, I do. Amended in the spec, in
   `AGENTS.md`-adjacent memory, and in the skill.
2. **The handoff got a schema and a gate** — his diagnosis: it is the only document read first every
   session and the only one with no rules, so an unfinished commitment leaves by simply not being
   mentioned. Built in the `woodev-docs-system` skill first, then projected here, at his instruction.
3. **The repo is private now.** He accepted GitHub Pages dying (Free plan, 0 consumers) once it was
   measured that hiding one file was theatre — all 433 `docs-internal` files were already public.

## TypeScript — asked, measured, answered

He asked whether writing the JS in TS from the start would have helped. Measured rather than
opined: `tsc --checkJs` on `location-cascade.js` gives 321 errors, of which **253 are noise** from
untyped `entry`/`record` and ~15 are substantive (all nullability). Scored against this session's
own defects, types would have caught **one of seven** (#526 C5) and none of the expensive ones —
ours live in the runtime semantics of third-party libraries and of the browser.

Decision: **TS for `src/` only** (the React bundles already build through `wp-scripts`, marginal
cost ≈ 0), enforced by a lint rule rather than intention (#542). **Not** for
`woodev/**/assets/js/frontend/`: those ship raw with no build, carry `0` import/export (IIFE +
globals), so types across the seams would be hand-written `.d.ts` — an unchecked transcript sitting
exactly where our bugs are — and a build step would break the verification that actually worked all
session, `grep`-ing the file the rig serves.

## Gates — earlier figures

**`main` after #526:** `composer check` **2787** / 6813 / **66 skipped**; jest **1474**.
**PR #535 (#530):** **2791** / 6826 / **66 skipped**; jest **1491** — measured in the PRIMARY
checkout, not the worktree. 19 new tests across two rounds, each proven to fail under a targeted
mutation. PR #533: 18 jobs pass, state CLEAN, merged. Docs lint OK (207 gotchas, session-start
budget within 120 KB).

## Related

- [../GOTCHAS.md](../GOTCHAS.md) — 206
- [../gotchas/a-select2-language-callback-that-returns-undefined-renders-blank.md](../gotchas/a-select2-language-callback-that-returns-undefined-renders-blank.md)
- [../gotchas/starting-codex-under-orca-needs-four-steps-not-one.md](../gotchas/starting-codex-under-orca-needs-four-steps-not-one.md)
- [s92.md](s92.md) — the session whose baselines were wrong
