> Написано в конце s113 (02.09.2026). Формат — `DOCS-SCHEMA.md` → Handoff Format, гейт в
> `npm run lint:docs`.
>
> ⚠ **ДЕРЕВО НЕ НА `main`.** Оно припарковано на **`kalbac/fix-163-list-anchor`** (`079dd89`) под
> его риговый проход по **PR #743**. Риг обслуживает рабочее дерево — это сделано намеренно.
> **Вернуть на `main`, как только проход закончится.** Один открытый PR — **#743**. 68 открытых
> карточек.
>
> **Гейты в ПЕРВИЧНОМ чекауте, sodium ВКЛЮЧЁН, измерено 02.09.2026 против `dc0c15c`:** unit
> **3401**/8348/**1**; jest **1610**/24; phpcs и phpstan чисто; интеграция **129**/506; **e2e 7/7**
> против живого рига. ⚠ Только с `php -d extension=sodium` и `rm -f .phpunit.result.cache` перед
> КАЖДЫМ прогоном; phpstan локально требует `--memory-limit=4G`.
>
> ⚠ На ветке #743 jest **1621** (+11). Это НЕ база `main` — не перепутать.

# Ждёт кнопки оператора

- **PR #743** (#163) — **риговый проход.** Дерево уже припарковано, риг отдаёт фикс. Я проверил
  сам: модалка **без поиска** даёт 3 строки с расстояниями (`1.0 mi`, `1.9 mi`, `1.9 mi`) по
  возрастанию; контроль на `main` — те же 3 строки и ни одного расстояния. **Не проверено живьём:
  что перетаскивание карты не переупорядочивает список** — закреплено только jest. Мили это
  английская локаль рига, не дефект.
- **#644** — расстановка приоритетов. Материал: `reviews/2026-08-31-644-prioritisation-material.md`.
- **#652** — сценарий 1 (единственный непроверенный).
- **#331** / **#332** — его собственное «сейчас не реализуем» от 15.08.2026.
- **#734** — держать ли ПЕРВОГО перевозчика на живом Яндексе. Вопрос не закрывался.
- **#737** — нужен ли ещё риговый mu-метод `woodev_test_pickup_shipping`. ⚠ Он переопределяет поле
  и бэкстоп ПЕРВОЙ фикстуры — сверить перед решением.

# Обязательства (перенос)

- **#736 #474 #111 #738** — **ЗАКРЫТЫ в s113** (PR #739 #740 #742). #738 заведена и закрыта в тот
  же день.
- **#163** — работа сделана в **s113** (PR #743), карточка ОТКРЫТА до его прохода.
- **#741** — НОВАЯ в **s113**, не начата. Учит фикстуру `test-cdek` Краснодару; **блокирует #150**.
- **#150** — НЕ бралась в s113 осознанно. Предусловие на риге есть (`?locality=Краснодар` → ровно
  1 точка), но `locality` берётся из РАЗРЕШЁННОЙ записи, а `test-cdek` Краснодара не знает →
  дойти путём покупателя нельзя. Сперва **#741**. ⚠ И даже после неё: текст #150 говорит «после
  того как карта будет полностью готова», а техдолг карты заморожен — решение брать её остаётся за
  ним.
- **#165** — своим текстом запрещает чинить вслепую и требует воспроизведения, которое прошлый
  исполнитель не смог построить. Осознанно не бралась в **s112**, и в **s113** по той же причине.
- **#706** — разобрана ПОЛНОСТЬЮ в s112, удаление НЕ выполнено, ждёт его слова. Метод сверки и SHA:
  `s112-706-branch-sweep.md` в скретчпаде s112. ⚠ Пересчитать перед удалением: `git branch --merged`
  под squash-мержем врёт втрое. В s113 ветки четырёх ворктри удалены вместе с ними.
- **#371** — JS и PHP по-разному решают, выбран ли ПВЗ (мультипакет); сверена в **s109**.
- **#573** — поздний `onSelect` (окно 10,5 с), нужен риговый повтор. **#182** — `(string)`-каст без
  `is_scalar` даёт `"Array"`; сверена в s109, но **в замороженном списке**.
- **#310** — переписана в **s110** под живой остаток. **#701** — решена в **s110**: запись, не работа.
- **#689** — отклонена осознанно в **s108**; брать только с потребителем. **#621** — пункт 1 закрыт
  в **s102**, остаток **ДЕРЖИТСЯ ЗА #639**.
- **#589** — решено в **s99**: шов «IP → координаты» со ВТОРЫМ потребителем; кандидат — кнопка из #310.
- **#567** — правило 1 закрыто в **s103**, остаток отложен 29.08.2026. **#247** — до релиза
  (**s108**). **#285** — так же (#409, **s83**).
- **#713 #714** — заведены в **s111** из сверки #695, проверены по коду, не начаты.
- **#710** (модалка «Создать заказ») и **#711** (ROI по доставке) — заведены в **s110**, ОТЛОЖЕНЫ и
  не в v1: без брейншторма в работу не брать.
- **PR-номера траншей s101–s113 — все смержены, кроме #743**: #607 #611 #612 #616 #619 #624 #625
  #626 #628 #629 #630 #631 #635 #636 #638 #645 #648 #649 #651 #655 #656 #658 #659 #660 #661 #663
  #664 #665 #666 #667 #685 #688 #690 #697 #698 #699 #700 #702 #703 #705 #716 #717 #718 #719 #720
  #722 #724 #726 #727 #728 #729 #730 #731 #732 #733 #735 #739 #740 #742.
- **Закрыто раньше, несу строкой ради гейта** — обязательство уходит только «сделано» или явным
  снятием оператором, никогда молчанием.
  s113: #736 #474 #111 #738. s112: #141 #725 #704 #270 #140 #171 #692. s111: #116 #707 #708 #709
  #721 #723 #694 #695 (#712 дубликатом). s110: #139 #289 (чинил PR #291). s109: #483 #374 #515 #437
  #503 #153 #155. s108: #676 #683 #686 #687 #653. s107: #321 #568 #510 #429 #318 #482 #322 #147
  #499 #673 #188. s106: #358 #356 #7 #8. s105: #410 #662. s104: #650 #646 #647 #361. s103: #353
  #627. s102: #632 #606 #609 #613 #514. s101: #598 #605 #594 #599 #610 #608. s100: #587 #591 #585
  #593 #592 #570 #595 #577 #596 #559 #560 #600 #603. s99: #397 #581 #451 #584 #518 #586. s98: #391
  #393 #396 #399 #400 #402 #444 #453 #473 #523 #524 #527 #532 #561 #563 #566 #571. s97−: #546 #555
  #554 #556 #558 #525 #530 #531 #535 #539 #542 #543. s96: #547 #552 #551 #553. s95: #512 #548 #529
  #541 #540 #534. s91−92: #494 #507 #508 #505 #502 #509. s83: #405. PR тех же траншей: #670 #679
  #668 #669 #671 #672 #674 #677 #678 #680 #681 (все s107).

# С чего начать

**Объём НЕ выбран.** Порядок мой, по цене и разблокировке.

1. **Вернуть дерево на `main`**, как только он закроет проход по #743 (смержить или дать правки).
   Это первое действие, а не фон: пока дерево на ветке, любой риговый замер меряет НЕ `main`.
2. **#741** — дешёвая фикстурная работа, разблокирует #150. Ровно тот же ход, что #734: научить
   `test-cdek` Краснодару, чтобы каскад доводил до него.
3. **#150** — только после #741 И только если он подтвердит (см. оговорку выше).
4. **#713 #714** — заведены в s111, проверены по коду, никем не тронуты.
5. **#589** — ждёт второго потребителя шва; кандидат назван (кнопка из #310).

## ⛔ Не трогать

**Техдолг карты #181 #152 #148 #182 #174 #173 #151 ЗАМОРОЖЕН** до пилотной миграции — подтверждено
им 02.09.2026. ⚠ #150 формально соседствует с этим списком.

# Что доказано замером

1. **Владение методом ПВЗ из хендлера вывести НЕЛЬЗЯ.** `Shipping_Method::get_plugin()` —
   `abstract protected`; токен хендлера (`$hook_prefix`) со списком его методов не связан:
   `woodev_realistic_shipping` против id плагина `woodev-realistic-shipping`, и
   `woodev-test-shipping-method` против метода `woodev_test_shipping`. Любой план «отфильтровать по
   плагину» упирается в это — проверять ДО начала работы.
2. **`private static` в классе, который каждый плагин строит себе сам, отвечает за ФЛОТ.** Флаг
   «уже сделал» на нём проверяет только первый плагин, молча. Готча
   `a-process-static-once-per-request-gate-checks-only-the-first-plugin`.
3. **Слой `checkout/` конфликты билдера РЕПОРТИТ, а не бросает:** 17 `_doing_it_wrong()` против
   одного `throw`, и тот — провал поиска. Прецеденты, когда уведомление уместно и когда нет,
   записаны в `class-checkout-handler.php:2469-2473` и `class-field.php:113`.
4. **`locality` модалки ПВЗ приходит из РАЗРЕШЁННОЙ записи, а не из поля города.** Нерезолвящийся
   город убирает параметр целиком и даёт пустой список на здоровом 200 — читается как сломанный
   перевозчик. Готча `the-pickup-modal-s-locality-comes-from-the-resolved-record-not-the-city-field`.
5. **`boundsChange` эмитится только на стратегии `viewport`.** Любой замысел «следовать за картой»
   на `bulk` не работает — именно поэтому живой центр карты в #163 отвергнут.
6. **Дефект очереди платёжной формы (#738) унаследован, а не привнесён v2** — в отгруженном v1 то
   же расхождение и тот же набор файлов на диске.

# Ловушки

1. **Сверять «риг не тронут» надо ПО СЕССИИ WooCommerce, а не только по user meta.** В s113 первая
   сверка сказала «байт-в-байт», а в сессии остался чужой `billing_city`, и следующий замер показал
   0 точек — что читалось как сломанный фикс. Запись потом восстановлена из бэкапа.
2. **Архивную спеку правит ПОМЕТКА, а не переписывание.** `docs-internal/archive/` — снимок того,
   что решили тогда. Воркер переписал §7.1, пришлось возвращать. Писать это в бриф.
3. **Тело PR закрывает карточки ЧЕРЕЗ ОТРИЦАНИЕ.** Шаблон `Refs #N — <что осталось>`, и
   **проверять после каждого мержа**: `gh issue view N --json state`. В s113 сработало правильно
   четыре раза подряд — шаблон в брифе работает.
4. **`gh project item-list` по умолчанию отдаёт 300 элементов, а на доске их 344** — свежие
   карточки в выдачу НЕ попадают, и это читается как «карточки нет на доске». Всегда
   `--limit 1000`. (Отдельно от известной ловушки, что `item-add` может вернуть 0 и не добавить.)
5. **`git stash` в ворктри с CRLF-мусором не возвращается по `pop`** — правки остаются в стеше, а
   `pop` молча делает вид, что применил. Доставать явным `git checkout stash@{0} -- <файлы>`.
6. **Serena игнорирует** `tests/** docs/** .github/** .ai/** .serena/** .claude/**` — там встроенные
   инструменты ЕДИНСТВЕННЫЕ, и это не нарушение правила. Вписывать в каждый бриф.
7. **Ворктри стартуют грязными с СЕМЬЮ CRLF-файлами**; удалять только через Orca.
   `worker-release` штатно отвечает `identity_unproven` и терминал оставляет — это документированная
   граница, а не сбой.

# Состояние на входе

| | |
|---|---|
| Дерево | ⚠ **`kalbac/fix-163-list-anchor`** (`079dd89`), НЕ `main` · Открытых PR — **1 (#743)** |
| Доска | **68** открытых; #741 в `Бэклог`, #163 в `В работе` |
| Готчи | **264** · ADR — **11** |
| Гейты (`dc0c15c`) | unit **3401**/8348/**1**; jest **1610**/24; phpcs и phpstan чисто; интеграция **129**/506; e2e **7/7** |
| Риг | ✅ Штатное, ДВА перевозчика; запись локации восстановлена из бэкапа и сверена |

# Related

- [CURRENT-STATE.md](CURRENT-STATE.md) — состояние проекта
- [sessions/s113.md](sessions/s113.md) — детали сессии
- [gotchas/a-process-static-once-per-request-gate-checks-only-the-first-plugin.md](gotchas/a-process-static-once-per-request-gate-checks-only-the-first-plugin.md) — главный урок s113
- [gotchas/the-pickup-modal-s-locality-comes-from-the-resolved-record-not-the-city-field.md](gotchas/the-pickup-modal-s-locality-comes-from-the-resolved-record-not-the-city-field.md) — почему #150 упёрлась
- [DOCS-SCHEMA.md](DOCS-SCHEMA.md) — формат этого файла и гейт к нему
- [GOTCHAS.md](GOTCHAS.md) — 264 готчи
