> 🌙 **АВТОНОМНАЯ ОВЕРНАЙТ-СЕССИЯ.** Оператор ушёл спать 08.08.2026, участия его не будет до утра.
> Утром — твой отчёт и его live-review по тому, что успеешь.
>
> **Мердж разрешён явно:** на pass по КАЖДОЙ джобе отдельно, `--squash --delete-branch`,
> **никогда `--auto`** (готча `feedback_avoid_gh_pr_merge_auto`, обжигались в s11 и s29).
> Утром он смотрит собранный `main` одним проходом, а не стопку веток.

# Состояние на входе

`main` = `4c63a02`. Открыт **PR #221** (#219, ленивая догрузка деталей) — зелёный, `CLEAN`,
**НЕ смерджен** по просьбе оператора «сохрани сессию пока без мержа». **Смерджи его первым
делом**, остальная очередь ложится поверх.

| | |
|---|---|
| Тесты | **1490 unit / 743 jest / phpcs чисто / PHPStan чисто** |
| Риг | поднят, `:8973`, драйвер **chrome-devtools MCP** |
| Стратегия рига | `WOODEV_TEST_PICKUP_STRATEGY=viewport`, `WOODEV_TEST_PICKUP_LIVE_YANDEX=false` |

# ⚠️ Три ловушки, до первой команды

**1. Риг отдаёт РАБОЧЕЕ ДЕРЕВО.** wp-env монтирует репозиторий: чекаут другой ветки мгновенно
откатывает правки первой, а переключение фикстуры убирает данные, на которых виден симптом.
За одну вчерашнюю сессию это дважды ввело оператора в заблуждение — в обе стороны. Прежде чем
просить кого-то посмотреть, назови ветку И константы. Готча
`rig-serves-the-working-tree-branch-switch-reverts-fixes`.

```bash
git branch --show-current
npx wp-env run cli wp config list --fields=name,value | grep WOODEV_TEST
```

**2. Локальный jest считает воркдеревья субагентов.** Достоверный итог только так:

```bash
npm run test:js -- --roots "<rootDir>/tests/js"
```

Проверять `ls .claude/worktrees/`, а не `git worktree list` — на Windows удаление падает и
оставляет каталог. Готча `jest-scans-agent-worktrees-inside-the-repo`.

**3. Никогда `npx jest`** — теряет jsdom, печатает фантомные падения. Готча
`npx-jest-bypasses-wp-scripts-jsdom`.

**Риг медленный, и это НЕ наш код:** `/wp-json/` ~16.7 с, запрос точек 7–10 с. Замерено.
Под viewport это и есть увеличительное стекло, вскрывшее всю очередь ниже.

# Очередь

## 1. Мердж PR #221

Проверь каждую джобу отдельно (`pass` + `CLEAN`), squash, удалить ветку, синхронизировать `main`.

## 2. #222 + #224 одним заходом — состояния дозагрузки

**Это самый крупный кусок и самый ценный. Дизайн согласован с оператором, не изобретай заново.**

**#222 — сайдбар не фильтруется при пане.** Причина подтверждена по коду: слушатель
`boundschange` в `_buildMap()` расходится по стратегиям, и viewport-ветка уходит в `return` до
`_emitVisibleChange()`. Под `bulk` мгновенный клиентский фильтр есть, под `viewport` — нет, и
сайдбар обновляется только когда придёт ответ сервера. Монтировщик слушает `visibleChange`
безусловно (`pickup-mount.js:2312`) — принимающая сторона готова, не хватает вызова.

**#224 — у дозагрузки нет своего состояния занятости.** Замерено: на ПЕРВОМ открытии
полноэкранный оверлей работает правильно (виден 10.0→17.5 с, спиннер по центру,
`elementFromPoint` возвращает сам спиннер). А любая последующая дозагрузка идёт **вообще без
индикатора**: один шаг зума → `REQ` 87746 мс, `RES` 97564 мс, **9.8 секунды** при скрытом оверлее
и трёх устаревших точках в списке. `clearInitialBusy()` одноразовый на цикл `start()` — оверлей
задумывался как «стадия 2 открытия карты», а не индикатор каждой дозагрузки.

**Решение оператора (08.08.2026), реализовать ровно так:**

1. **Полоска прогресса по верхней кромке КАРТЫ, во всю её ширину — всегда.** Не в сайдбаре: под
   viewport покупатель чаще всего возит карту со свёрнутой панелью, и индикатор в сайдбаре в этот
   момент не виден вовсе. Место уже фактически есть: карта держит верхнюю полосу 64px под
   поисковый бар (`map.margin`), сам бар с `top: 16px` — полоска ~3px по `y = 0` ложится над ним.
2. **Оверлей со спиннером на сайдбаре — ТОЛЬКО когда открыт СПИСОК.**
3. **Открытая КАРТОЧКА точки остаётся живой.** Разобрано и принято: карточка про конкретную точку,
   дозагрузка по bbox её содержимое не меняет; накрыв её, мы прятали бы адрес и часы работы,
   которые человек читает, перекрывали кнопку выбора на ~10 с и накладывались на ВТОРУЮ блокировку
   из #223.
4. **Полноэкранный оверлей остаётся только для ПЕРВОГО открытия** — там показывать нечего вообще,
   и замер подтверждает, что он работает верно.

## 3. #223 — CTA активна, пока летит догрузка деталей

Недосмотр вчерашнего #219: окно между открытием карточки и приходом вердикта ничем не закрыто,
точка всё это время держит разрешительный-по-умолчанию вердикт из разреженного листинга. Оператор
кликнул в это окно: ушёл `/select`, ответ вернулся, **и ничего не произошло** — это отдельная
часть карточки, требует замера.

Блокировать CTA со спиннером на время полёта `/points/{id}`. Примитив есть —
`Panels.prototype.setSelectionBusy()` вешает `is-locked`, — но семантика у него другая («идёт
подтверждение»), решить при постройке. **Освобождать по ЛЮБОМУ исходу, включая отказ**, иначе
неудачная догрузка запрёт карточку навсегда — ровно ловушка, на которой s53 уже обожглась со
стражем устаревания.

Свести с #224 в ОДНО состояние на двоих, чтобы два спиннера не дрались.

## 4. #225 — вердикт не применяется с первого раза после восстановления

Оператор сам пометил: подтвердить на 100% не смог, риг тормозит. **Разбирать замером, не
рассуждением** — инструментировать `refreshPointDetails()`/`updatePoint()` и снять фактическую
последовательность: что было в `cardPointId` на момент ответа, нашёл ли `updatePoint()` точку,
приходил ли листинг между. Три непроверенные гипотезы — в теле карточки. Возможно, одна причина
с #223.

## 5. Живой источник Почты РФ — начать

**Решение оператора: берём.** Даёт viewport-глубину, которой три статические точки не дают.

**Проверено по референсу, не по памяти:** `plugins-reference/woodev-russian-post` точки **не
забирает** — он встраивает виджет Почты (`ecomStartWidget`, `assets/js/frontend/map.js`) и
получает выбор через callback. Points-API оттуда взять неоткуда. Живой источник = разбор бандла
`https://widget.pochta.ru/map/main.a7d147fb5267ec1f0932.js` (ссылку дал оператор).

Образец для подражания — `class-test-live-yandex-point-source.php` (#185): opt-in константой,
токен из константы и НИКОГДА в файле, кэш на транзиенте, fail-soft через `Woodev_API_Exception`,
докблок с ЗАМЕРЕННОЙ формой ответа, а не с документацией.

⚠️ **Что бы ни нашлось в бандле — ключи и идентификаторы Почты в репозиторий не попадают.**
Формулировка задания для исполнителя должна быть БЕЗУСЛОВНОЙ: «не коммить X». Условная форма
(«не коммить, как будто это наш секрет») однажды уже разрешила коммит. Готча
`public-repo-third-party-credentials`.

Это самый непредсказуемый по времени пункт — **брать последним**. Если упирается, лучше честный
отчёт «разобрал столько-то, эндпоинт такой-то, дальше нужен ключ/решение», чем ночь без
результата.

## Необязательное, если останется время

- **#214** — дубль префикса «стрстр.1»/«стрк.1» у 5post. Оператор взял, вариант 2 (схлопывать
  только точный дубль). Косметика фикстуры, не фреймворка.
- **#220** — сырой код `card` в способах оплаты viewport-фикстуры. Тот же класс.
- **Мобильный проход** по всему, что наделала s55 (иконки, табы, чипы, акцент, погашенный зум) —
  проверялось только на 1280px, а s50 находила дефекты, видимые исключительно при ресайзе.

# Правила, которые вчера стоили ошибок

**Карточку — ДО работы.** Настоящий промах не «забыл подвинуть», а «сделал без карточки вообще»
(так уехал зум, пришлось заводить #217 задним числом). Перед первым коммитом: какая это карточка?
Нет — создай. `Бэклог` означает НЕ СДЕЛАНО.

**Замер, а не рассуждение.** Вчера моя гипотеза в теле #224 (гонка порядка → зависший спиннер) не
воспроизвелась: реальная причина оказалась другой. Инструментируй риг и снимай факт.

**Проверяй гипотезу ДО отчёта.** Первый замер «пан не даёт запроса» был промахом измерения — не
дождался успокоения камеры; повтор дал ровно 1 вызов.

**Исполнителям явно давать право противоречить заданию.** Работает четвёртую сессию подряд.
Вчера тесты дважды поймали мои же ошибки до рига: подписку на `zoomChange` нельзя вешать рядом с
`panels.on('zoom')` (там `provider` ещё `null`, и `start()` его пересоздаёт), а
`if (typeof provider.on === 'function')` — лишний шум, `on()` часть контракта провайдера.

# Внешняя память

**Supermemory не подключён** с s47, **obsidian-mcp-tools** отвалился в s55. Авторитетны
`docs-internal/` и `~/.claude/projects/*/memory/`.
