> Написано в конце s68 (12.08.2026), автономный овернайт. Прошлая сессия исполняла план
> location-провайдера: **блоки PR-A и PR-B отгружены и смерджены**, задачи 1–9 из 16.
> **Эта сессия — блок PR-C: клиентский слой.**

# 🎯 Главное: PR-C, задачи 10–12 (клиент)

**Спека:** `docs-internal/specs/2026-08-12-location-provider-design.md` — D1–D15, решения приняты
оператором лично, переиграть нельзя. **План:** `docs-internal/plans/2026-08-12-location-provider-plan.md`.
Обе читать до первой строчки кода; исполнителям **явно разрешено противоречить плану, когда код с
ним расходится** — доложить, не молча следовать (правило s52; за s68 сработало шесть раз).

- **Задача 10** — `location-typeahead.js`: нейтральный комбобокс, прогрессивное улучшение (нативный
  `<input>` НЕ подменяется — attach/detach должны быть чистыми, арбитраж перезапускается на каждой
  смене страны), дебаунс 250 мс, отбрасывание устаревших ответов по счётчику поколений, отрисовка
  через `textContent` и никогда `innerHTML`, клавиатура ↓/↑/Enter/Esc. Ванильный, без jQuery внутри.
- **Задача 11** — `location-cascade.js`: граф полей **поверх существующего стора §8**
  (`checkout-field-store.js`), а не параллельный мир состояния. Персист по D8 (`POST /select`, затем
  свой `update_checkout`), обратное заполнение из одной записи без второго запроса, очистка потомков
  только ВНИЗ и только через гейт запомненного значения родителя, postcode пишется но ничего не
  чистит, attach/detach по поддерживаемости страны.
- **Задача 12** — арбитраж с родным WC Address Autocomplete: клиентская обёртка записей реестра
  (`canSearch()` отдаёт `false` для НАШИХ стран, остальное делегирует) + серверный kill
  `woocommerce_address_providers → []`, когда все страны магазина ⊆ страны провайдера.

**Мердж-политика: это UI-блок. Код пишем, PR открываем, но НЕ мержим** — ждёт твоей ручной
проверки на риге. Риг-чеклист лежит в задаче 16 плана (7 сценариев).

Режим: subagent-driven (worker Sonnet + критик Codex). Он окупился: 6 находок, все реальные.

---

# Состояние на входе

**`main` = `7819c10`, дерево чистое, по слою открытых PR нет.**

| | |
|---|---|
| Тесты на `main` | **1911 unit / 4875 ассертов / 895 jest**, phpcs чисто |
| Риг | `:8973`/`:8974`, живой Яндекс, Москва/77 |
| Serena | работает, требует `activate_project('woodev_framework')` |
| Готчи | **129** |

**Что уже есть на сервере и ждёт клиента (всё смерджено, покрыто тестами):**

- `Location_Service` — единственная точка входа: `is_active()`, `get_customer_record()`,
  `set_customer_record()`, `resolve_for( $plugin )`, `is_country_supported( $country, ?$level )`,
  `get_supported_countries()`, `provider_for_level( $level )`.
- REST `woodev/v1/location/suggest` (публичное чтение, рейт-лимит, `q` 2–128, неизвестный `within`
  = отсутствующий, НЕ ошибка) и `/select` (нонс `wp_rest`, полная запись через
  `Location_Record::from_array()`, ответ `{ current: { key, level }, persisted: bool }`).
- Блок `location` в конфиге чекаута: `endpoints`, `nonce`, `countries`, `mode` (пока честно
  `typeahead`), `levels`, `current`, `implicit`. **Id провайдера в конфиге нет ни одного** — клиент
  узнаёт только, какие уровни обслуживаются; это закреплено тестом, не сломать.
- Ассеты клиента подключаются под `file_exists()`-гейтом (`Checkout_Handler`), так что задачам 10–11
  править enqueue не придётся — файлы просто появятся.

---

# ⚠️ Ловушки, до первой команды

Ловушки s65–s67 в силе (пикер на `/classic-checkout/`, jest только
`npm run test:js -- --roots "<rootDir>/tests/js"`, wp-env из корня, оба мира событий jQuery, три
генерации таймеров в харнессе classic-адаптера, модульное состояние `pickup-mount.test.js`).
Новое, дорого купленное в s68:

1. **Три дефекта подряд были невидимы локально и видны только на CI.** «Зелено локально» на этом
   репозитории не является утверждением. Конкретно: рефлексия без `setAccessible` (локальный PHP
   8.5 против матрицы 7.4–8.3 — готча `reflection-setaccessible-version-guard`), gitleaks на
   правдоподобной заглушке (готча `a-no-leak-test-needs-a-low-entropy-placeholder`), и
   `do_action('init')` в интеграционном тесте (дополнение к
   `integration-test-global-admin-hooks-output-and-submenu-accumulation`).
2. **Записанное правило не путешествует в делегированную работу само.** Готча про `setAccessible`
   лежала в индексе с июня, и исполнитель наступил на неё снова — бриф на неё не ссылался. Правила,
   релевантные задаче, вписывать В БРИФ, а не рассчитывать на индекс.
3. **`within` в `/suggest` резолвится через ТЕКУЩУЮ запись покупателя,** а не по ключу — для каскада
   «регион → город» это корректно (каждый выбор персистится по D8), но в режиме связанного списка
   (задача 13) регион может быть не персистирован, и скоуп молча деградирует до общестрановой.
   Если задача 13 упрётся — вот сюда смотреть.
4. **Каскад чинить только гейтом запомненного значения родителя** — холостой программный `change`
   не должен разрушать значение потомка (готча
   `a-programmatic-parent-change-must-not-run-a-destructive-cascade`, куплена в s66 порчей сессии).
5. **select2 сообщает выбор через jQuery `.trigger('change')`, который НЕ создаёт нативного
   события** — привязка в обоих мирах, и хотя бы один jest-тест на путь продюсера через РЕАЛЬНЫЙ
   jQuery (готча `jquery-trigger-change-fires-no-native-event`; в s66 семь зелёных тестов
   промахнулись мимо этого).

---

# Очередь после PR-C

- **PR-D (задачи 13–16):** режимы select2, дефолтный НП, pickup (#159), clean-break чистка. Тоже
  риг-гейт.
- **Ждут ОПЕРАТОРА, не мерджить самому:** **PR #277** (#265, нужен embedded-режим) и **PR #279**
  (#274 п.1) — висят с s66/s67. При мердже второго — конфликт в шапке `GOTCHAS.md`: оставлять обе
  записи changelog, счётчик **130**.
- **Ждут решения оператора:** **#288** (санитайзер логов API маскирует только `Authorization` —
  три варианта, база разделяется с платёжным деревом), **#285** (VERSION=2.0.1 против 157
  аннотаций `@since 2.0.2`), **#274 п.2–3**.
- **В бэклоге:** #289 (мультистрановая цепочка опишется неточно — сегодня недостижимо), #247
  (минификация + чистка мёртвых ассетов перед v2), #245, #270 (фикстура рига знает один город —
  помешает риг-чеклисту).

---

# Мелочь, замеченная но не тронутая

`AGENTS.md` требует «Internal docs — English only, no Russian», но `CURRENT-STATE.md`,
`next-session-prompt.md` и часть `SESSION-LOG.md` давно и последовательно пишутся по-русски (их
читает оператор). Правило и практика разошлись; чинить надо одну из сторон — кандидат в ближайший
аудит документов, не трогал в исполняющей сессии.

---

# Внешняя память

- **Serena** — `activate_project` + правки существующего PHP через built-in `Edit` (CRLF).
- **Supermemory** — работает, OAuth; запись s68 добавлена.
- **obsidian-mcp-tools** — требует открытого Обсидиана; `sessions/latest-context.md` обновлён s68.
