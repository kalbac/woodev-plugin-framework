> Написано в конце s98 (27.08.2026). Формат — `DOCS-SCHEMA.md` → Handoff Format, гейт в
> `npm run lint:docs`.
>
> **Дерево на `main` (`1a5cebf`), чисто. Ворктри — два критик-ворктри s98, их можно сносить.**
>
> ✅ **CI РАБОТАЕТ. Репозиторий снова ПУБЛИЧНЫЙ с 27.08.2026** (решение оператора). У публичных
> репозиториев на стандартных раннерах минуты бесплатны и квоту не расходуют, поэтому блок снялся
> сразу — замерено: тот же `Markdown Lint`, что падал за 2 с, прошёл за 14 с сразу после
> переключения. **Мержить можно как обычно.**
>
> Что было, чтобы не диагностировать заново: Free-план даёт **2000 минут Actions в месяц**, было
> израсходовано **1800**, дальше блокировал бюджет $0. **≈1042 из них сожгла сама s98** — 187
> прогонов, 27 биллинговых минут на полный цикл PR. Приватность с 25.08 и включила счётчик.
> Симптом читается как красная сборка: все джобы падают за 2 секунды, включая `Label PR`, и
> **лога у них нет вовсе**. Готча
> `every-ci-job-failing-in-two-seconds-is-a-billing-block`, карточка **#583**.
>
> **Правило про бюджет Actions для приватных репозиториев записано в глобальный `CLAUDE.md`**
> (раздел «GitHub Actions budget»), вместе с замером по локальному CI: `run-local-ci` гоняет тот
> же workflow на официальном раннере, но **на нативной Windows не работает** (проверено на
> 0.18.1), нужен WSL.
>
> **Гейты на `main`, измерено 27.08.2026 в ОСНОВНОМ чекауте:** `composer check` **2950** / 7111 /
> **66 skipped**; jest **1545** в 21 сьюте; интеграция в контейнере **124** / 485.
>
> **Бюджет чтения — 128,8 KB из 176.** Потолок поднят в s97 (#556), крутить второй раз НЕЛЬЗЯ.

# Ждёт кнопки оператора

- **#581** — ошибки полей мастера установки (#397). Код готов, локально зелено. Видимый
  результат — **красное поле** на шаге мастера, а харнесса на это в репо нет. **Нужен его
  проход по ригу**, потом мерж.
- **#567 (НОВАЯ)** — на каком языке msgid. **305 кириллических против 453** латинских, замер и
  правило классификации на карточке. Английские = переписать 305 строк и завести каталог;
  русские = ноль работы, но расходимся со стандартной практикой. Решать ему.
- **#570 (НОВАЯ)** — `Settings_Provider::create()` берёт секции нетипизированно, поэтому отброс
  молчаливый. Фатал закрыт, вопрос об эргономике публичного API — нет.
- **#577 (НОВАЯ)** — частота nopriv-лога скриптовых событий. Инъекция закрыта, флуд нет; механизм
  для переиспользования назван (`Rest_Rate_Limit_Trait`), но он в шипинг-неймспейсе, а это ядро.
- **#559**, **#560**, **#437** — без изменений с s97.

# Обязательства (перенос)

- **#518** — выбор ПВЗ снимает «неявность» записи. Решено в **s92**, **не начато**. В силе.
  Замерить, нужна ли серверная половина. Видно покупателю → **строить можно, мержить нельзя.**
- **#514** — **m6 и T3 закрыты в s98 (#563).** В силе остаток: **m4** (контраст WCAG AA) и
  **m5** (ширина селектора) — оба UI, ждут рига.
- **#353** — **начат и осознанно откачен в s98**, замер объёма на карточке. Правило (~40 строк)
  готово, но валит 82 юнит-теста, и у четырёх «провайдер без НП» — сам субъект. Сначала решить,
  дефект ли страно-слепой `provider_for_level()`, потом включать.
- **#473** — **ЗАКРЫТ в s98 (#571).** Второй пункт карточки оказался недостижим — записано.
- **#451** — единственная не взятая из «мелочей» s97. #444 и #453 закрыты.
- **Остаток ревью 27B — ЗАКРЫТ ЦЕЛИКОМ в s98:** #391, #393, #396, #397 (PR держится), #399,
  #400, #402.
- **Остатки слоя локаций:** #356, #358, #361, #410 — заведены в **s78**, в силе, не разбирались.
  #353 оттуда же — см. выше.
- **Закрыто в s98:** #391, #393, #396, #399, #400, #402, #444, #453, #473, #514 (m6+T3), #523,
  #524, #527, #532, #561, #566.
  **Закрыто в s97:** #546 (PR **#555**), #554 (PR **#556**), #514 m1+m2 (PR **#558**).
  **В s96:** #547 (PR, **#539** ч.3 — оператор принял на риге) и #552 (закрыл **#551** и **#553**).
  **В s95:** #512, #525, #529, #531, #542.
- **Закрыты раньше, несу строкой ради гейта** (обязательство уходит только «сделано» или явным
  снятием оператором, никогда молчанием): **#494**, **#507**, **#508**, **#505** — s91;
  **#502**, **#509** — s92; **#534** — s93; **#530** (PR **#535**), **#540**, **#541**,
  **#543** — s94; **#405** — код s83, риг-замер s95.

# С чего начать

1. **#581** — единственный открытый PR. Попросить оператора пройти по ригу, потом смержить.
   **#582 смержен** 27.08 после того, как публичность вернула CI: 18 pass + 1 skip, CLEAN.
2. **#451** — не-UI, мержится сам. Единственная развилка внутри: редактировать reason-фразу при
   присвоении (одно место, меняет семантику поля) или на каждой границе логирования.
4. **#518** — строить можно, мержить нельзя.

⛔ **Не брать:** #567, #570, #577, #559, #560, #437 (решения оператора), и стоящий список
#474, #483, #511, #515, #331, #332, #374. Отложено до релиза: #285, #247.

# Что доказано замером

1. **CI вставала в `02:49:17Z` по КВОТЕ, не по коду, и снята публичностью в 06:40Z.** Всё смерженное
   за ночь до #579 включительно прошло настоящую зелёную CI (19/19, у docs-PR 20/20), каждый со
   статусом CLEAN перед мержем. Без проверки только #581 и #582.
   **Цена ночи замерена:** 187 прогонов, 27 биллинговых минут на полный цикл PR (CI 16 +
   Integration 10 + Triage 1, по таймстампам джоб с округлением вверх — `/timing` API отдаёт
   нули и не годится), итого **≈1042 минуты**. Два источника, которые можно срезать, если
   оператор решит: **34 из 78** прогонов CI+Integration — это `push` в `main`, повтор того, что
   PR уже доказал (≈440 мин/мес); и docs-коммит гоняет полную матрицу Integration (27 минут
   вместо 1). Обе правки трогают мерж-гейт → его решение, детали на #583.
2. **Щель ниже `minimumInputLength` недостижима для русских названий** (#523): 1329 меток НП из
   четырёх регионов `test-cdek`, 1212 уникальных, **минимальная длина 3**. Порог НП = 2, щель
   требует длины 1. Решение записано в докблок `minimumInputLengthFor()`, объём доказательства
   назван (один провайдер, четыре региона RU).
3. **msgid: 305 кириллических против 453** (#567). Правило классификации на карточке. Мой
   первый счёт (300/458) был НЕВЕРЕН — обход матчил только `T_STRING` и пропускал 7 корневых
   вызовов с ведущим обратным слешем, которые PHP 8 отдаёт одним `T_NAME_FULLY_QUALIFIED`.
4. **`mb_substr()` маскировал дефект санитайзера.** Критик нашёл щель по регекспам в изоляции и
   заключил, что байт доезжает до лога; прогон целой функции показал `c328419b42 -> 3f28413f42`.
   Обе половины вместе объясняют, почему это важно: без mbstring маски нет, а `ext-mbstring` в
   `composer.json` не объявлен.
5. **`provider_for_level()` страно-слеп по умолчанию** (#353). Настоящий гейт страну передаёт
   (`class-location-service.php:972`), а `:1160` — нет. Из-за этого «объявил НП, но не отдаёт
   его в стране» невыразимо на слепом пути.

# Ловушки

1. **Локальный PHP 8.5, пол CI — 7.4.** `ReflectionProperty::setValue()` на приватном свойстве
   требует `setAccessible( true )` ниже 8.1 и ругается deprecation с 8.5. Зелёный
   `composer check` здесь — свидетельство про 8.5 и ни про что больше. Готча
   `the-local-php-is-four-versions-above-the-ci-floor`.
2. **`git checkout -- <file>` выбрасывает несохранённую работу.** При фальсификации пользуйся
   `git stash push -- <file>` плюс `git stash pop`; `checkout` во время такого прохода стоил мне
   переделки всех правок в одном файле.
3. **Сырые управляющие байты, записанные экранированием, перекодируются.** Байт CSI доехал до
   файла как валидный UTF-8 `c2 9b`, и каждый кейс молча перестал проверять свою ветку.
   Строить через `chr( 0x9b )`. И `preg_match` с флагом `u` возвращает **FALSE, не 0**, на
   невалидном UTF-8.
4. **`parse_str()` теряет информацию** — строит массивы, нормализует ключи (точка и пробел в
   подчёркивание). Сравнивать две query-строки через него нельзя: любая потеря РАСШИРЯЕТ
   совпадение.
5. **Тест, который сам себе готовит предусловие, не проверяет производителя.** Удали скрытый
   input из вьюхи — четыре теста гварда останутся зелёными, а редактирование токенов в админке
   молча перестанет сохраняться.
6. **Проверять отдаваемое ригом надо В КОНТЕЙНЕРЕ, а не на диске.**
7. **`composer check` и jest — не одно дерево.** `rm -f .phpunit.result.cache` перед каждым
   замером; jest только из bash с `--roots`, никогда `npx jest`.

# Состояние на входе

| | |
|---|---|
| Дерево | **`main`** (`1a5cebf`), чисто |
| Открытые PR | **#581** (ждёт риг) — единственный |
| CI | ✅ работает — репозиторий снова публичный с 27.08.2026, минуты не тратятся |
| Ворктри | `s98-critic-563`, `s98-critic-569` — можно сносить |
| Готчи | 223 |
| Гейты | `composer check` **2950** / 7111 / 66; jest 1545 / 21; интеграция 124 / 485 — измерено 27.08.2026 после мержа #582 |
| Бюджет доков | 128,8 / 176 KB |
| Критик | **kilo**, `kilo/openai/gpt-5.6-sol-discounted`. Круг по крупному PR — $1,5–3,5, а не $0,01–0,03, как записано в s97: та цифра была за маленькую задачу |
| Репо | **PUBLIC** снова с 27.08.2026 (приватным был 25.08–27.08); Pages снова доступен, `docs.yml` пока отключён на `push` |

**Риг:** провайдер `test-cdek`, регион «Предустановленный список» (`related-list`), НП «Список с
поиском» (`ajax-select2`), `default_locality_policy = fixed` («Москва», `test-cdek:44`),
`woodev_location_allow_custom_settlement = no`. `wp_woodev_popular_settlements` засеяна: по 3
записи на Москву (`r81`) и Санкт-Петербург (`r82`).
**Риг отдаёт рабочее дерево** — переключаешь ветку, оставь её до конца прохода и проверь замером
(`grep -c` В КОНТЕЙНЕРЕ), а не памятью. Дерево сейчас на `main`.

```bash
MSYS_NO_PATHCONV=1 docker exec -w /var/www/html/woodev-framework -e TEST_SUITE=integration \
  de59f74e6d3d19d18a7f7b6608fda7e7-tests-cli-1 \
  sh -c 'rm -f .phpunit.result.cache; vendor/bin/phpunit --testsuite=Integration'
```

# Related

- [CURRENT-STATE.md](CURRENT-STATE.md) — состояние проекта
- [sessions/s98.md](sessions/s98.md) — детали сессии
- [DOCS-SCHEMA.md](DOCS-SCHEMA.md) — формат этого файла и гейт к нему
- [GOTCHAS.md](GOTCHAS.md) — 223 готчи
- [wiki/orchestrating-agents-with-orca.md](wiki/orchestrating-agents-with-orca.md) — рецепт запуска kilo критиком
