# Current State — Woodev Plugin Framework

> Lean state doc: phase status, open bugs, next actions. **Full session history → `SESSION-LOG.md`** (newest on top). Program history snapshot → `platform-v2-program-tracker.md` (rewritten s60); active program map → `specs/2026-06-25-shipping-module-decisions.md`.
> Last updated: 2026-08-12 (s67 — **БРЕЙНШТОРМ ПРОВЕДЁН, ДИЗАЙН ПРИНЯТ, СПЕКА + ПЛАН СМЕРДЖЕНЫ.** `main` = `e19b079`, дерево чистое. Интерактивная сессия с оператором (Fable 5, кода не писали — так решено заранее). Итог по #273+#127+#159: **единая точка входа для данных о населённых пунктах** — один провайдер на магазин (DaData батарейкой во фреймворке, токен серверный), обязательный адаптер в каждом плагине (запись → идентичность карьера), ключ `provider_id:native_id`, ленивый резолв с сессионным кэшем, дуальный стор записи (обобщение `WC_Edostavka_Customer_Location_Data`), по-страновой арбитраж с родным WC Address Autocomplete (замерено по trunk: он address_1-only и плющит идентичность — новая готча). Спека `specs/2026-08-12-location-provider-design.md` (D1–D14) + план `plans/2026-08-12-location-provider-plan.md` (16 задач, 4 PR-блока: A контракты → B DaData+REST → C клиент → D режимы/дефолт/pickup/чистка) — PR #282, CI 20/20, смерджен. Ссылки оставлены в карточках. Тесты не менялись: 895 jest / 1595 unit. Готчи 127 → **128**. **Ждут оператора по-прежнему: PR #277** (#265, нужен embedded-режим) **и PR #279** (#274 п.1). **🎯 NEXT = исполнение плана, блок PR-A (задачи 1–6)** — subagent-driven, worker Opus/Sonnet + критик Codex, Serena обязательна в каждом PHP-брифе.)
> Prior: 2026-08-11 (s66 — **ТРИ ДЕФЕКТА ИЗ ПРОХОДА ОПЕРАТОРА ЗАКРЫТЫ, ДВЕ ПОСТАНОВКИ КАРТОЧЕК ИСПРАВЛЕНЫ ЗАМЕРОМ.** `main` = `18da9f0`, дерево чистое. Полностью автономная сессия (оператора не было с первого сообщения). Смерджены **#275** (#272), **#276** (#271), **#278** (#143); закрыты #272, #271, #143, #178. **Ждут оператора: PR #277** (#265) и **PR #279** (#274, пункт 1 из 3) — визуальные, не мерджил. Тесты на `main`: **895 jest / 1595 unit / 4259 ассертов**, phpcs чисто. **#272: механизм из карточки неверен, и это выяснилось до кода.** Виноват не takeover-конвертер, а КАСКАД: он синхронно обнулял DOM и стор на холостом программном `change` от WooCommerce, а стартовый `update_checkout` самого WooCommerce уносил пустоту в `WC()->customer` через 51 мс — за 2.9 с до ответа источника, после чего сервер отдавал пустое поле на всех загрузках. Никакого `change` по потомку при этом не летит вовсе, то есть предложенная в карточке правка не изменила бы ничего. Починено гейтом на ЗАПОМНЕННОМ прежнем значении родителя, двумя раздельными записями (`resolved` — значение, `fetched` — набор опций). Попутно в той же функции: `.done()` писал в узел, захваченный до запроса и подменённый takeover'ом в полёте. **У этого адаптера не было ни одного jest-теста** — отсюда и живучесть бага; добавлено 7, убиты все 7 мутаций. **#271: риг поправил ПЕРВУЮ ВЕРСИЮ ФИКСА.** jQuery `.trigger('change')` не создаёт нативного события, а select2 сообщает выбор города именно так — делегированный `addEventListener` не срабатывал, при семи зелёных тестах, которые все дергали нативный `Event`. Теперь привязка в обоих мирах событий. Риг подтвердил полный цикл на живом Яндексе: выбор в Москве → регион 78, поле очищено и подпись вернулась → назад в Москву + перезагрузка, ТА ЖЕ точка `019373a0…` восстановлена сервером (доказательство, что сессионная карта цела). **#265: флаг, убирающий слой UI, глушит все ветки, которые через него докладывали** — второй случай семейства (#260 был той же формой на ветке ожидания); обе ветки отказа теперь говорят через оболочку диалога (`showNotice`, не `showError`). Не проверено на риге осознанно — нужен embedded-режим и искусственный отказ живого карьера. **#274 остановлен после пункта 1 сознательно:** пункты 2 и 3 вкусовые, а **постановка пункта 3 неверна** — `#shipping_method` лежит ВНУТРИ `tr.shipping td`, значит нынешний якорь и есть `review_order_after_shipping`, а не хватает `after_shipping_rate`; в карточку записаны три варианта с рекомендацией. **Доска: ни одной из четырёх карточек сессии на ней не было вообще** — все добавлены и расставлены. Готчи 125 → **128**, плюс переписана рекомендация в `session-key-vs-order-meta-prefix` (#143). **Готовлено к следующей сессии:** она **открывается брейнштормом** по идее оператора про поля «Населённый пункт»/регион/адрес и провайдеров данных — карточки #273 + #127 + #159 связаны как одна проблема, под разговор написан брифинг `research/2026-08-11-locality-field-brainstorm-brief.md` (только факты, без предложений). **Главная находка брифинга: шов нормализации адреса УЖЕ ПОСТРОЕН и не подключён** — `Address_Normalizer` (`suggest()`/`normalize()`), `Null_Address_Normalizer`, `Shipping_Plugin::get_address_normalizer()` есть в class-map при НУЛЕ call sites, плюс зарегистрированные и никем не подключаемые ассеты DaData; то есть #127 планирует как будущую работу форму, которая уже на диске. **🎯 NEXT = брейншторм**, затем твоя проверка PR #277 и #279.)
> Prior: 2026-08-11 (s65 — **#176 СДЕЛАН, ПРОВЕРЕН ОПЕРАТОРОМ И СМЕРДЖЕН (PR #269); #178 ЗАКРЫТ ЗАМЕРОМ.** `main` = `e64c0b8`, дерево чистое, веток и открытых PR нет. **Проверка оператора на риге дала пять замечаний вокруг пикера** — заведены #271 (кнопка врёт при смене региона) и #272 (takeover-поле стирает своё же значение и пишет пустоту в сессию WC; там же сверка его разбора про `WC_Edostavka_Customer_Location_Data`), плюс UI-пачка из трёх пунктов ждёт оформления. Тесты **1595 unit / 4259 ассертов** (было 1550), jest 880 без изменений, phpcs чисто. Сессия: брейншторм с оператором, дальше автономный овернайт, subagent-driven (worker Sonnet, critic Codex). **Премиса карточки подтверждена ДО кода четырьмя независимыми ногами:** `WC_Checkout::get_value()` для ключа не из `billing_*`/`shipping_*` проваливается в пустоту (фильтр `woocommerce_checkout_get_value` не подцеплен нигде в `woodev/`), в `WC()->session` точка не писалась, клиентского хранилища во фреймворке нет вовсе — то есть ненаписанный персист, а `restoreSelection()` был верен всегда. **Цена оказалась выше, чем в карточке:** при `replaceAddress` адрес пункта уезжает в РОДНЫЕ поля WC, которые WooCommerce персистит сам, поэтому после перезагрузки покупатель видел адрес на месте при пустом id и заблокированном заказе. **UI восстановления уже существовал и был мёртв** — докблок `syncTriggerLabel()` дословно рассчитан на этот персист; JS в итоге не тронут ни строкой. **#178 отвечен исходниками WC 11.0.0:** асимметрия гость/авторизованный реальна, но не там, где предполагала карточка — `save_data()` пишет только при `has_session()`, а её безусловно удовлетворяет `is_user_logged_in()`; гостю нужна корзинная кука, и **на чекауте это состояние недостижимо**, поэтому дуальный стор (как `WC_Edostavka_Customer_Location_Data`) здесь не нужен. **Форма взята из его же боевого кода:** СДЭК уже держит карту `[city_code][type]`, Почта РФ — один слот со сторожем по ФИАС, отчего и теряет точку при возврате в прежний город; три плагина — три несовместимых словаря локации (ФИАС / `city_id` / `geo_id`), поэтому ключ объявляет домен через новый сем `Selection_Scope`. **Ревью дало четыре находки, ни одной косметической:** мои две (экшен нёс доисправленную точку; пустой ключ писался и читался как обычный, из-за чего чужая точка могла восстановиться) и Codex одну Medium (дробная крышка `0.5` складывалась в `0` и снимала лимит) — все три починены с мутационным доказательством. **Первый прогон Codex оказался половиной ревью** (песочница не поднялась, тесты он не видел и честно сказал) — перезапуск инлайн-бандлом дал 8 находок по тестам, включая то, что все ключи в тестах строчные и мутант с нормализацией доменных ключей проходил весь сьют. **Риг подтвердил сценарий оператора целиком** на `/classic-checkout/` с живым Яндексом: 77 → выбор → перезагрузка → id **в атрибуте `value` серверной разметки**, 78 → пусто, обратно 77 → та же точка. Заведены 5 готчей (119 → 124). **🎯 NEXT = #272** (самый неприятный: ломает любое takeover-поле), затем #271, UI-пачка, #265.)
> Prior: 2026-08-11 (s64 — **ПЯТЬ PR СМЕРДЖЕНО, КАРТА ФУНКЦИОНАЛЬНО ЗАКРЫТА, ПРИНЯТО КОНТРАКТНОЕ РЕШЕНИЕ О ПРОИЗВОДНЫХ ПОЛЯХ.** `main` = `89d213a`, дерево чистое, **открытых PR нет**. Тесты **880 jest / 1550 unit**, phpcs чисто. Смерджены пять PR: #261 (#259), #262 (#248), #255 (#252), #264 (#260), #266 (#263 — проверен оператором на риге уже после сохранения сессии). Работа шла в диалоге с оператором, не овернайт. **#259:** слушатель `message` переехал до `appendChild` (после рукопожатия #251 пропуск первого сообщения = мёртвый пикер), плюс закрыт вакуумный проход гейта источника (`event.source === contentWindow` проходил, когда обе стороны `null`); оба теста проверены мутацией; **проверено на риге живым виджетом Почты end-to-end** (рукопожатие → `POST /api/pvz 200` → выбор → наш `/pickup/.../select 200` → поле записано → модалка закрыта). **#248 закрыт ЗАМЕРОМ, а не правкой:** непустой пул влечёт непустой `lastBbox` по построению (единственный писатель пула — приземлившийся листинг; `lastBbox = null` только в `start()`, который двумя строками ниже чистит пул и двигает поколение). Правка из карточки ничего бы не изменила наблюдаемо и стоила бы инварианта #232/#238. Путь, который разбор s63 НЕ покрывал (сторонний провайдер отдаёт ложный bbox), закрыт слоем ниже: datasource опускает не-4-элементный `bounds` → запрос без режима адресации → `Point_Query::from_request()` отвергает. **#260 (оверлей занятости под `ownsChrome`) сделан и проверен оператором**, включая доработку по его замечанию — задержка 500 мс, чтобы виджет успел показать собственную заблокированную кнопку; hit-тест на риге доказал, что оверлей физически перехватывает второй клик (`elementFromPoint` в центре возвращает оверлей), 20 сэмплов через 7853 мс. **#245 оказался ложной тревогой:** прод-ключ вшит ещё 12.06.2026 (коммит `fdde793`), а `QSis…` из CURRENT-STATE — ключ ЛОКАЛЬНОГО издателя `:8090`, и ключи обязаны различаться (генерируются лениво в каждом окружении). Осталась одна читающая команда на проде. **Контрактное решение оператора (принято):** поле, являющееся производным видом обязательного, выводится НА ГРАНИЦЕ, а не в месте показа; поле без источника вывода остаётся опциональным, и его отсутствие — информация. Заведены **#263** (адрес в строке поиска — реализован и смерджен), **#265** (под `ownsChrome` обе ветки отказа немые — Бэклог). Локальные ветки почищены: было 6 лишних, осталась 0. **🎯 NEXT = овернайт: брейншторм #176** (персист выбранного ПВЗ через перезагрузку чекаута — самая весомая карточка карты), затем #265.)
> Prior: 2026-08-10 (s63 — **#251 РЕШЁН ЗАМЕРОМ, EMBEDDED-ШОВ ВПЕРВЫЕ ЗАРАБОТАЛ НА ЖИВОМ КАРЬЕРЕ.** `main` = `a109ca5`, дерево чистое, из открытых PR только **#255** (визуальный, ждёт оператора). Тесты **1546 unit / 863 jest**, phpcs чисто. Овернайт, subagent-driven (worker Sonnet, critic Codex). **#251: развилка решена замером, а не выбором** — виджет Почты это iframe на `https://widget.pochta.ru/map/` плюс шим на 1.2 КБ, постящий внутрь `postData` и ловящий `pvzData`; фреймится откуда угодно, **нашу песочницу не ломает** (единственный аргумент за script-режим не подтвердился), оба наших гейта (`origin` строгим `===`, `event.source === iframe.contentWindow`) держатся на живом карьере. Отсюда **адаптер поверх iframe**: гейт остаётся во фреймворке, домен — в двух опциональных хуках плагина. **Побочно вскрылось главное: в `pvzData` нет ни координат, ни имени** (подтверждено и замером, и доками), то есть `normalizePoint()` требовал `lat`/`lng` от карьера, который их не шлёт, унаследовав требование от нашей же карты, которой под `ownsChrome` нет; координаты сделаны опциональными-но-валидируемыми, `Pickup_Point::from_array()` НЕ тронут. **Codex: 0 Critical/High, 3 Medium, 1 Low** — главная находка: hex-строка координаты валидировалась через `Number()`, а конвертировалась `parseFloat()` → точка тихо уезжала в нулевой меридиан; это расхождение знали с #201 и оставили как недостижимое, а послабление координат сделало его боевым. Low про `null`-координаты **отклонён с обоснованием** (`isset()`-семантика + живой payload Почты шлёт `null`). **Риг: пять проверок карточки закрыты наблюдением** — панелей 0, `refresh()` под `ownsChrome` жёсткий no-op (WC ответил, `woodev/v1` 0 вызовов), двойное подтверждение = **2 раундтрипа / 1 применён**, таймаут отработал на **10159 мс**; полный путь успеха прошёл до записи поля и закрытия модалки. **Фикстура не доходила до сервера, и её докблок это скрывал** — `fetch_details()` зовётся всегда, независимо от провайдера карты. **#158 и #244 закрыты проверкой, а не работой:** #158 отгружен ещё 05.08 в `f96a7dc` без `Closes`, #244 помечен в сабплане как CANCELLED 04.06 — экстракцию построили, замерили (база 1514 → **1520**, критерий карточки в минус) и отвергли, сохранив её тесты, готчу и исправление трекера. Заведены **#259** и разбор в **#248**. **🎯 NEXT = #255 ждёт твоей проверки на риге**, затем #248 (нужен подтверждающий тест), #259, #247/#245.)
> Prior: 2026-08-10 (s62 — **#238 ЗАКРЫТ И ПРОВЕРЕН НА РИГЕ, НАЙДЕН ДЕФЕКТ ВЕРСИОНИРОВАНИЯ МОДАЛКИ, SUPERMEMORY ПОЧИЩЕН.** `main` = `fb97f59`, **дерево чистое, веток и открытых PR нет** — оба PR сессии смерджены (#249 → #238, #246 → #243). Тесты **1520 unit / 808 jest** (+2), phpcs чисто, PHPStan чисто; CI на PR #249 — 19/19 pass, `CLEAN`, на нужном SHA. **Serena и Supermemory работают** — проверены живыми вызовами; Serena потребовала не только рестарта, но и ручного `activate_project` (хэндофф s61 обещал «только рестарт»), запись «Supermemory не подключён с s47» снята как устаревшая. **#238: токен `echoExpected` удалён, а не починен** — `isSelfRefreshInFlight()` читает уже существующий `refreshWaiter`, который гасится на всех путях, поэтому у подавления больше нет собственного времени жизни и H2 исчезает по построению; зависимость от порядка привязки описана комментарием. **H1 оказался независимо ненаблюдаем — это замерено:** у `updated_checkout` нет происхождения, предложенный сценарий даёт одно обновление и до, и после правки; тест оставлен характеризующим. **Риг подтвердил три сценария** (живой Яндекс, 813 точек): смена корзины при открытом пикере → перезапрос, собственное эхо → без перезапроса, смена после эха → снова перезапрос. `refresh()` отработал в продакшен-форме впервые. **Исправлен харнесс jest**, который записывал `one()` не исполняя, то есть проверял не тот случай. **Заведён #250** — `woodev-modal.js` версионируется голой `self::VERSION`, правки файла не сбрасывают кэш браузера (его же стилю тремя строками ниже дали `filemtime()`); стоило ложного сигнала на риге. **Заведён #251** — embedded-провайдер ни разу не гонялся, фикстуры нет; решение оператора: виджет брать у Почты РФ из его боевого плагина. **Supermemory: удалено 33 записи**, включая боевой доступ к панели 3x-ui; причина мусора найдена — Hermes с `flush_min_turns: 6` и `write_approval: false`; `forget` не берёт чанкованные записи. **#243 проверен оператором на риге и смерджен**; проверка вскрыла **#252** (снятая галка красится в `muted` и читается как заблокированная — сигнал инвертирован, Инбокс). **#214 закрыт оператором как not planned.** **🎯 NEXT = #251** (embedded-провайдер: сначала построить фикстуру, виджет брать у Почты РФ).)
> Prior: 2026-08-09 (s61 — **#243 ДОВЕДЁН ДО PR, #238 ОСТАНОВЛЕН НА ДВУХ HIGH, ПРИЧИНА ОТВАЛА SERENA НАЙДЕНА.** Сессия оборвана 5-часовым лимитом посреди второго круга правок по #238. **Дерево НЕ чистое: ветка `feat/238-cart-change-invalidation`.** Тесты 1520 unit / 806 jest / phpcs чисто / PHPStan чисто. **Две ветки:** `feat/243-filter-last-type-disabled` → PR **#246**, CI полностью зелёный (19 job'ов, `mergeStateStatus: CLEAN`), **ждёт ручной проверки оператора на риге** — не мерджить; `feat/238-cart-change-invalidation` → PR нет, три коммита, последний `wip(...) DO NOT MERGE`. **Serena: причина найдена, фикс применён, нужен только рестарт** — `uvx --from git+…serena` пересобирает пакет на старте при каждом апстрим-коммите, не укладывается в дефолтные 30 с, Claude Code рвёт соединение; тот же лог с другим SHA есть от 05.08, то есть s55–s60 она падала именно так. Применён `MCP_TIMEOUT=120000` в глобальный `~/.claude/settings.json`. **#238 несёт два подтверждённых HIGH от Codex:** голый булев `echoExpected` съедает первое пришедшее `updated_checkout` (H1) и остаётся взведён навсегда, если WooCommerce не ответит (H2 — подтверждается докблоком самого файла про спаренный `REFRESH_TIMEOUT_MS`). Согласованный фикс — токен удалить и читать уже существующее состояние `refreshWaiter`/`refreshTimer` в момент события. **Спека была выполнена идеально и всё равно оказалась неверной** — spec-ревью вернуло «COMPLIANT», а выбранный ею механизм содержал оба дефекта. **#214: замер сделан** по полному живому набору (813 точек) — правило B+ чинит 93%, ломает 0 корректных адресов; постановка карточки неверна (её вариант чинит 28% и мимо крупнейшего класса); ждёт решения оператора. **Заведены #247** (минификация CSS/JS, Инбокс, карточка оператора) **и #248** (viewport `refresh()` чистит пул без перезапроса — дыра стала живой после #238, Бэклог). **Ложный P0 от Codex стоил целого прогона** — транспорт бандла через аргумент PowerShell срезает одинарные кавычки; готча дополнена. **🎯 NEXT = доделать #238** (см. `next-session-prompt.md`), затем ждать оператора по #243/#214.)
> Prior: 2026-08-09 (s60 — **ПОЛНЫЙ АУДИТ ДОКУМЕНТОВ (первый с s13!) + РИГ ПЕРЕВЕДЁН НА ЖИВОЙ ЯНДЕКС.** Код не менялся, тесты прежние: **1520 unit / 800 jest**. Коммиты `48e4b6c` (аудит: ~150 находок, 137 файлов), `0d09689` (Serena), `bd38155` (CI), плюс коммит сохранения этой сессии; дерево чистое, PR и веток нет. **Аудит пятью проходами** (4 читающих агента → 4 редактора → финальный): гейтвеи синхронизированы (регистрация `Woodev_Loader::register()` вместо тумбстоуна s27, jest/build-команды вписаны как мердж-гейты, WP-минимум 6.6 везде, clean-break в QWEN.md вместо мёртвого deprecation-цикла), CURRENT-STATE ужат 193→91 строк, GOTCHAS 402→299 (счёт исправлен: 114 готчей/21 неймспейс, `[shipping/pickup]` получил свою секцию), ADR-007 «classic-JSX» supersede-блок (иначе агент внёс бы удалённый в s36 хак обратно), трекер v2 переписан (был мёртв с s13 и противоречил сам себе), 65 отгруженных планов/спек/ревью → `archive/` с полным свипом ссылок. **Хэндофф s59 ошибался о собственной истории:** «последний аудит был в s39» — реально s13. **Заведены #244** (2 из 4 экстракций P4 не сделаны при «пройденном» гейте) **и #245** (прод-ключ верификатора — плейсхолдер, release-blocking); оба проверены по коду до создания, третий кандидат (протухшие i18n-маркеры) НЕ подтвердился и не заведён. **Решение оператора: Serena ОБЯЗАТЕЛЬНА** («меньше ошибок, экономия токенов») — правило ужесточено в трёх доках: проверять подключение на старте, докладывать при отсутствии, вписывать в каждый PHP-бриф субагенту; **сейчас Serena НЕ подключена — чинить окружение**. **Риг за сессию: живая Почта → фикстура → живой Яндекс bulk** (`WOODEV_TEST_PICKUP_LIVE_YANDEX=1` побеждает Почту и STRATEGY; 812 точек по Москве; включение через `wp config set` без рестарта). **Разблокирован замер #214:** токен Яндекса читается из корня репо — «нечитаемость» s59 была cwd-эффектом wp-env (новая готча `wpenv-resolves-environment-from-cwd`). **🎯 NEXT = #243** (решён, можно брать), **#238** (нужен дизайн), **#214 ждёт оператора** (замер теперь возможен).)
> Prior: 2026-08-09 (s59 — **#234 НАКОПЛЕНИЕ ТОЧЕК СМЕРДЖЕНО, РЕШЕНИЕ ПО КРЫШКЕ ПРИНЯТО ЗАМЕРОМ.** **1520 unit / 800 jest / phpcs чисто / PHPStan чисто.** `main` = `0cd51c4`, **дерево чистое, открытых PR и веток нет**. Смерджены #239 (#234), #240 (#220) и #241 (мобильный оверлей — **оператор отревьюил лично: «багов не обнаружил, всё работает чётко»**). Закрыты #220, #230, #233 (обе половины), #234, #237, #242. **Постановка #234 была неверна дважды и это выяснилось до кода:** докблок `setPoints()` уже отдавал межзапросную дедупликацию вызывающему (правка целиком в `pickup-mount.js`, контракт `Map_Provider` не тронут), а карта с сайдбаром разойтись не могут (сайдбар = пул ∩ кадр). **Крышка не поставлена — по замеру:** пул 20 000 точек это `setPoints()` 334 мс и 8.8 МБ против листинга в 6–13 СЕКУНД, суперлинейный `setTypeFilter` (1017 мс) на viewport-путь не попадает вовсе; реально накапливается 1290 точек за тур по Москве и области, 4664 — за четыре области на `MIN_ZOOM`. Выведен фильтр `woodev_pickup_max_accumulated_points` (0 = без ограничения), потому что плотность точек — знание домена. **Критик Codex отработал дважды (по дизайну и по коду) плюс повторное ревью моих правок:** по дизайну — ложное «листинг побеждает» и отсутствующий генерационный барьер; по коду — пять находок, включая вытеснение по `Object.keys()` (это НЕ порядок вставки для числовых id Почты — выбрасывалась самая новая точка) и `(int)` на возврате фильтра, превращавший массив в `1`. **Возражение исполнителя вскрыло #238:** `getSession()` не вызывается из продакшена, значит `forgetPointDetails()` из #232 там не работал никогда; радиус мал — сессия пересоздаётся на каждое открытие модалки. **Риг:** зум 14 → сервер отдал 31 точку, на карте 350, в сайдбаре 25; зум +1/−1 → 348/348 сразу, до ответа сервера. **Мобильный:** оверлей сайдбара закрывал 94% экрана, снят — полоска прогресса уже лежит на верхней кромке сайдбара. **#230 закрыт замером** (0 срабатываний ловушки на 671 строку лога под увеличенной нагрузкой), ловушка снята из контейнера. **#214 НЕ сделан осознанно** — вариант 2 не покрывает собственные примеры карточки, нужен замер разброса. Заведены #237, #242. **#242 закрыт по решению оператора («ничего не делаем») ПОСЛЕ проверки его довода замером:** усечение самозалечивается при зуме внутрь — зум в район даёт точки, которых не было в урезанных 2000 (Бутово +1, Митино +3, Некрасовка +4 при листингах 16/24/29), то есть недостижимых точек нет; плюс 2000 точек на широком кадре рисуются 24 DOM-объектами. **🎯 NEXT = #238** (мёртвый `refresh()`), **#214 ждёт ответа оператора**. s60 делится на 10 — предложить аудит документов.)
> Older per-session headers (s42–s58) → `SESSION-LOG.md` (they were verbatim duplicates).

## Last session context (≤3 lines)

- **s67 (12.08.2026) — БРЕЙНШТОРМ: ЧУЖОЙ ГОТОВЫЙ API ПРОВЕРЯЕТСЯ ПО ИСХОДНИКАМ ДО ТОГО, КАК ЛЕЧЬ В ОСНОВУ ИДЕИ.** Оператор предложил «взять за основу WC Address Autocomplete Provider» — замер по trunk показал, что его механика каскад нести не может (address_1-only, плоские строки, очистка невозвращённых полей), но его ФОРМА (per-country арбитраж, общая настройка провайдера) подтвердила идею; итог — свой контракт, сосуществование по-странно́. Второй урок: **дилемма «три режима поля» растворилась через разделение данные/презентация** — режимы оказались рендерерами над одним источником, гейтящимися capability провайдера. Третий: **его боевые плагины ответили на половину вопросов дизайна** (дуальный стор, `update_checkout` после персиста, ленивость резолва) — референс в его же коде снова оказался сильнее рассуждений.

- **s66 (11.08.2026) — ПОСТАНОВКА КАРТОЧКИ МОЖЕТ БЫТЬ НЕВЕРНА В САМОМ МЕХАНИЗМЕ, А НЕ В ДЕТАЛЯХ.** Урок сессии: #272 называл виновником takeover-конвертер и предлагал не отправлять `change` — а виноват каскад, и никакого `change` там не летит вовсе, то есть предложенная правка не изменила бы ничего. Второй, и он дороже: **риг поправил не карточку, а МОЮ ПРАВКУ** — семь зелёных мутационно-проверенных тестов дергали нативный `Event`, а select2 сообщает выбор через jQuery `.trigger('change')`, который DOM-события не создаёт; в jsdom естественно написать именно то, чего продакшен не делает. Третий: **у слоя, который ломался на каждой загрузке, не было ни одного теста** — отсюда живучесть. Четвёртый: **флаг, убирающий слой UI, глушит все ветки, которые через него докладывали** (второй случай того же семейства после #260), и форма `if ( panels ) { … } return;` читается как защита, а ведёт себя как потерянный исход. Пятый: **ущерб задаёт расписание ЧУЖОГО актора** — обнуление на 3 секунды пересеклось с 50-миллисекундной сериализацией формы WooCommerce и стало постоянной порчей сессии.

- **s65 (11.08.2026) — ПОСТАНОВКА КАРТОЧКИ ПОДТВЕРДИЛАСЬ, А ЕЁ ЦЕНА ОКАЗАЛАСЬ ВЫШЕ ЗАЯВЛЕННОЙ.** Урок сессии: «выбор молча теряется» было **мягче** правды — замена адреса пишет в РОДНЫЕ поля WC, которые персистятся сами, поэтому страница выглядела так, будто выбор цел, а заказ не оформлялся. Проверять надо не только «верна ли премиса», но и «полон ли ущерб». Второй: **половина ревью выглядит как всё ревью** — Codex честно написал, что его песочница не поднялась и тестов он не видел, но вердикты по прочитанному выдал уверенно; перезапуск инлайн-бандлом дал 8 находок, которых в первом прогоне не было в принципе. Третий: **тесты на непрозрачный ключ, написанные строчными буквами, не проверяют непрозрачность** — мутант, нормализующий доменные ключи, проходил весь сьют. Четвёртый: **пустая строка из доменного сема — это не значение, а отказ отвечать**, и принятая за ключ она восстанавливает точку из чужого города. Пятый: **эталон был в его собственном боевом коде** — и оба плагина решают задачу по-разному, что и объяснило требование «домен объявляет свой primary key» лучше любого рассуждения.

- **s64 (11.08.2026) — ФОЛБЭК, ПОВТОРЁННЫЙ В N МЕСТАХ ПОКАЗА, ДРЕЙФУЕТ; ВЫВОД НА ГРАНИЦЕ — НЕТ.** Урок сессии: `short_address || address` стоял в строке сайдбара и отсутствовал в строке поиска 170 строками ниже, а комментарий в том же файле про этот фолбэк **знал** — правило записали и применили к одной половине. Починка «добавить фолбэк во второе место» сохранила бы форму, которая и породила баг. Второй: **дефект, который нельзя воспроизвести, стоит опровергать построением** — #248 закрыт замером (непустой пул влечёт непустой `lastBbox` по построению), а правка из карточки стоила бы соседнего инварианта. Третий: **охранник для недостижимого состояния — спекулятивный код**: добавил защиту от «ретрай посреди подтверждения», потом проверил достижимость и убрал, оставив на её месте разбор. Четвёртый: **тест на длительность, который прокручивает весь интервал, проходит и при нуле** — мутация `500 → 0` не уронила ничего, пока границу не закрепили с обеих сторон (`n-1`/`n`). Пятый: **карточка release-blocking может быть ложной** — #245 сравнивал прод-ключ с ключом локального рига, а они обязаны различаться по конструкции.

- **s63 (10.08.2026) — ПОСТАНОВКА КАРТОЧКИ ПРОВЕРЯЕТСЯ ДО ЗАПУСКА ИСПОЛНИТЕЛЯ, А НЕ ПОСЛЕ.** Урок сессии: из четырёх взятых карточек **три оказались с неверной премисой** — #251 обещал фреймабельный URL (оказался шим вокруг iframe), #158 был уже отгружен посторонней работой, #244 описывал «не сделано» там, где стояло документированное CANCELLED с живыми причинами. Стоило это ноль потерянных прогонов ровно потому, что каждая сверялась с кодом перед работой. Второй: **безобидное расхождение становится боевым от соседней правки** — hex-строку в координатах знали с #201 и оставили как недостижимую, а послабление `lat`/`lng` превратило её в тихую подмену точки на нулевой меридиан. Третий: **отменённое решение стоит перепроверять построением, а не рассуждением** — экстракцию #244 собрали целиком, и она сама показала, что база растёт. Четвёртый: **докблок может скрывать дыру убедительнее, чем её отсутствие** — фикстура утверждала, что `Point_Source` под embedded не используется, и из-за этого два новых переключателя были мёртвыми. Пятый: **делегированная развилка = требование замера** — оператор сказал «реши сам, но не догадками», и решающим оказался ровно один замер (песочница виджет не ломает), снявший единственный аргумент за альтернативу.

- **s62 (10.08.2026) — ЗАПИСАННОЕ ПРАВИЛО ПРИМЕНИЛИ К ОДНОЙ ПОЛОВИНЕ ПАРЫ.** Урок сессии: `woodev-modal.js` версионируется голой `self::VERSION`, а его же стиль тремя строками ниже — `filemtime()`, и правило там сформулировано верно. Правильное рассуждение записали и применили к одной половине общего хэндла. Второй: **«сервер жив» ≠ «инструмент работает»** — Serena поднялась и вернула `No active project`; проверять надо символьным вызовом, а не фактом подключения. Третий: **дефект может быть независимо ненаблюдаем, и это выясняется замером** — H1 проходит на обоих механизмах, потому что у `updated_checkout` нет происхождения; честнее записать это в спеку, чем сделать вид, что тест что-то доказал. Четвёртый: **тестовый дубль может проверять не тот случай** — jQuery-дубль записывал `one()` не исполняя, то есть все тесты про эхо моделировали страницу, где WooCommerce не отвечает никогда. Пятый: **чистка памяти без перекрытия источника бессмысленна** — Hermes сбрасывает переписку каждые 6 ходов.

- **s61 (09.08.2026) — СООТВЕТСТВИЕ СПЕКЕ НЕ ЕСТЬ КОРРЕКТНОСТЬ.** Урок сессии: по #238 spec-ревью вернуло «SPEC COMPLIANT, ноль находок», а Codex на том же коде — два HIGH, потому что неверен был **механизм, выбранный самой спекой**, а не его исполнение. Второй: **два ревьюера могут спорить о разном** — ревьюер доказывал, что токен не залипает между сессиями, Codex — что залипает внутри одной открытой; усреднить их было бы ошибкой, надо смотреть, на какой вопрос отвечает каждый. Третий: **ложный P0 критика — признак сломанного транспорта**, если ошибка синтаксиса заявлена в контекстных строках, которых никто не трогал; испорчена вся аргументация прогона, а не одна находка. Четвёртый: **дрейф от правила снова оказался сломанным окружением, и это доказано** — Serena падала по таймауту сборки, лог лежал на диске с s55; гипотеза проверялась пять шагов и на каждом могла остановиться на правдоподобном неверном ответе. Пятый: **замер в третий раз подряд опроверг постановку карточки** (#214 — «дубль префикса» оказался меньшинством случаев).


## Program status (high level)

| Stage | Status | Notes |
|---|---|---|
| S0 Platform Split | ✅ DONE | tag `platform-v2-split-done`; base platform-neutral, resolver minimal, clean-break Phase 3 shims deleted |
| S1 Shipping | ✅ DONE | PR #20; PSR-4 module; rate/packing seam + conformance audit (s2–s4) |
| S2 Box-packer | ✅ DONE | PR #21/#22; woven into rate-calc single-seam template |
| S3 Licensing | ✅ DONE | need-license (PR #25) → React UI (PR #31) → webhooks + Ed25519 signing (PR #35) |
| Remote-deactivation UX | ✅ DONE | s10–s12; command cycle proven live (push prod + pull rig); B-13/14/15 resolved |
| Checkout field layer (§8) | ✅ DONE | merged s44 (PR #132 → `957c039`) |
| Shipping SP-track | 🚧 IN PROGRESS | SP-1…SP-5 done (настройки, auth+секреты, валидация, show_if, карта/ПВЗ — incl. pickup selection + viewport accumulation); SP-6…SP-11 pending; map = `specs/2026-06-25-shipping-module-decisions.md` |
| S4 EDD / S5 React admin / S6 ecosystem | ⚪ deferred | post-v2.0 |

`composer check` green at s66: **1595 unit / 4259 assertions / 895 jest** on `main` (jest 880 → 895: +7 for the §8 classic adapter, which had none, +8 for the pickup locality watcher). phpcs + PHPStan clean, integration green. Keep green after each change. Note: PHPStan crashes locally on Windows (`-1073741819`, environmental — gotcha `phpstan-windows-parallel-worker-segfault`); Linux CI "Run PHPStan" is the authoritative gate.

## Phase Status (subsystems)

| Phase | Code | Browser-verified | Notes |
|-------|------|------------------|-------|
| Framework Core | ✅ | ✅ | Bootstrap, Plugin base, Lifecycle — stable |
| Payment Gateway | ✅ | ✅ | `class-payment-gateway.php`: **~3,542 lines** (whole tree ~13.8k); trait-extraction candidate |
| Shipping Method | ✅ | ✅ | PSR-4 namespaced |
| Licensing | ✅ | ✅ | EDD store integration; React license page on core `woodev/v1` REST |
| Settings API | ✅ | ✅ | Typed settings framework |
| Settings React page (SP-1) | ✅ | ✅ | `Woodev > Настройки`: registry + `woodev/v1/settings` REST + React surface on the UI-kit |
| Setup wizard (UK-3/4) | ✅ | ✅ | React wizard on the shared UI-kit (s41, PR #99) |
| Box Packer | ✅ | ✅ | Shipping box-packing algorithm |
| REST API | ✅ | ✅ | Plugin REST routes |
| PHPStan | ✅ | — | Level 3, **no baseline** (`phpstan-baseline.neon` removed; do not reintroduce) |
| Documentation | ✅ | — | Two-tier: `docs/` (GH Pages) + `docs-internal/` (AI agents) |

## P6 gate evidence — base is platform-neutral & not a god-object (reference)

- **Platform neutrality:** base `Woodev_Plugin` declares **zero** WooCommerce/HPOS-named methods; the last HPOS seam (`is_hpos_compatible()`) was removed. Late-safe WC hooks live in `Woodev\Framework\Woocommerce_Plugin::register_woocommerce_hooks()`; early `before_woocommerce_init` feature declarations are wired by the bootstrap from loader `supported_features` metadata. Enforced by `PlatformNeutralBaseHasNoWcMethodTest`, `PlatformNeutralRestApiTest`, `BootstrapRegistrationTest`.
- **Base size (2026-06-04):** `woodev/class-plugin.php` ~1,274 lines / 74 methods (56 public) after the P6 split-done follow-up.
- **Construction shape:** `__construct()` is an ordered list of `init_*_handler()`/`load_*` calls ending with `add_hooks()`; `add_hooks()` wires only base-owned hooks.

## Known Bugs / Open debt

- [⚠️] `class-payment-gateway.php` ~3,542 lines — trait-extraction candidate (grooming, s13; → `FUTURE-BACKLOG` (frozen — см. board №6)).
- **B-2 loader-protocol forward-tolerance — standing behavior (s27):** the resolver loads framework classes from the **highest registered copy for the whole fleet** regardless of the bootstrap rendezvous winner; the `backwards_compatible` min-version guard deactivates-with-notice any plugin below the loaded copy's min. Standing rules (every loader definition MUST set `version` + `backwards_compatible`; registration contract additive-only from v2.0.0) → `AGENT-RULES.md` Rule 3.
- [ℹ️ OB-7 follow-up] «Плагины» still shows discontinued/coming-soon items (Беру.ру/GOODS) — `edd-api/v2` exposes no `_coming_soon`/`_product_icon`/rating; needs a woodev.ru-side API extension (s22 task #1). Framework normalizer already consumes them forward-compatibly.
- **All earlier release-blocker findings RESOLVED** (2026-06-01 audit, PHPStan masks, base-class leaks, eCheck/ACH removal, payment-gateway base-method regression, etc.) — see `SESSION-LOG.md` + git history. Not repeated here.

### Public-docs API staleness — DEFERRED (operator decision, s13)

- `docs/` (GH Pages) registration examples still teach the **v1 `register_plugin( '1.4.0', ... )` positional API**, which in v2 is a **tombstone** (quarantines the caller, never registers). The live API is `register_loader_definition([...])`. Examples also hardcode `'1.4.0'`/`VERSION='1.4.1'` instead of the `%%FRAMEWORK_VERSION%%` placeholder / `2.0.1`. Affected: `getting-started.md`, `core-framework.md`, `payment-gateway.md`, `shipping-method.md`, `README.md`.
- **Operator decision (s13): do NOT touch public docs yet** — he is currently the only consumer of the framework; the public docs get rewritten once everything is fully ready. Recorded so it is not mistaken for an oversight.

## Next Actions

0. **🚀 Исполнять план location-провайдера, блок PR-A (задачи 1–6).** Спека `specs/2026-08-12-location-provider-design.md`, план `plans/2026-08-12-location-provider-plan.md`. Subagent-driven (worker Opus/Sonnet + критик Codex), Serena обязательна в каждом PHP-брифе. Блоки B→C→D дальше по плану; PR-C и PR-D заканчиваются риг-проверкой оператора.
1. **Проверить PR #277 (#265) и PR #279 (#274 пункт 1) на риге** — оба визуальные, CI зелёный, оставлены оператору. #277 без embedded-режима не проверить. При мердже второго из них — конфликт в шапке `GOTCHAS.md`: оставлять обе записи changelog, счётчик теперь **129** (s67 поднял базу до 128).
2. **#274, пункты 2 и 3** — нужен выбор оператора; постановка пункта 3 исправлена замером, три варианта в карточке.
3. **#245** — ждёт одной читающей команды на проде.
4. **#270** — фикстура рига знает только один город; мешало трижды за s66, всплывёт и на риг-чеклисте плана (сценарии со сменой локации).

<details>
<summary>Выполнено в s65 (было пунктом 1)</summary>

Премиса подтвердилась замером и оказалась даже мягче правды (замена адреса персистится сама, поэтому страница выглядела целой при заблокированном заказе). Реализовано, проверено на риге, отдано в PR #269.

~~#259~~, ~~#248~~, ~~#252~~, ~~#260~~, ~~#263~~ — смерджены в s64.

</details>

**Техдолг и улучшения карты (181, 159, 152, 148, 182, 174, 173, 151) осознанно НЕ трогаем до пилотной миграции** — пилот на живом карьере покажет, какие из этих карточек реальны, а какие мы придумали сами.

Deferred (всё остальное — board №6): UK-CFR (settings extensibility) и прочие отложенные карточки живут на доске; `FUTURE-BACKLOG.md` заморожен.

## 🔔 Cross-Project Reminder — Ecosystem Orchestration (dormant)

- **Trigger:** v2.0.0 shipped AND stable in production for several weeks. When it fires, surface it in the session-opening summary; do **NOT** auto-start; point the operator to the spec and read its "Prompt for the Future Agent" section first.
- **Spec:** `D:\Projects\woodev_theme\docs\superpowers\specs\2026-05-13-woodev-ecosystem-orchestration-spec.md`. Cross-ref: `FUTURE-BACKLOG.md` → "Cross-Project Initiatives" #7 (frozen — см. board №6).

## Local rig

- **The picker lives on `/classic-checkout/`, NOT `/checkout/`** — the latter is the BLOCK checkout (the adapter is SP-11, unbuilt), where there is no `form.checkout`, no `carrier_pickup_point` and no trigger, which reads as a broken build rather than the wrong URL. Product id `12` fills the cart via `?add-to-cart=12`. Gotcha: `rig-checkout-url-is-the-block-checkout`.
- **s65: the working tree is left on `feat/176-pickup-selection-persistence`,** so that is what the rig serves until someone checks out another branch — name the branch before asking anyone to look (gotcha `rig-serves-the-working-tree-branch-switch-reverts-fixes`).
- **Project rig: dev `:8973` / tests `:8974`** (chrome-devtools MCP driver). Ports live in the gitignored `.wp-env.override.json`; the `WOODEV_TEST_*` constants live in the **container's wp-config** (`wp config set` — survives restarts) and are mirrored in `.wp-env.override.json`. **State as of s60: LIVE YANDEX bulk ON** — `WOODEV_TEST_PICKUP_LIVE_YANDEX=1` wins over `WOODEV_TEST_PICKUP_LIVE_POCHTA=false` and `WOODEV_TEST_PICKUP_STRATEGY=viewport`; the rig serves 812 live Yandex points (Moscow). Fixture is active only when both live flags are false. **s63:** `WOODEV_TEST_POCHTA_ACCOUNT_ID` / `WOODEV_TEST_POCHTA_ACCOUNT_TYPE` were added to the container's wp-config (operator-supplied Отправка credentials — never committed) so `WOODEV_TEST_PICKUP_EMBEDDED` can drive the live Почта widget. That switch itself was **removed again after verification**, so the rig is back on live Yandex; set it to `1` to re-enter embedded mode.
- **Issuer `:8090` — KEPT, do NOT touch.** It is effectively a copy of prod (woodev_theme = local woodev.ru + EDD SL + deactivator, with test data); operator uses it independently. Container `c8ec47a5...-wordpress-1`. Authority pubkey `QSisoK0CDOmIOqGHvilMe+4mB/LMRFHf9hi6BxatfMk=`. The rig-only `zz-rig-host-rewrite.php` mu-plugin was **removed in s28** (from the container's `wp-content/mu-plugins/` and the `woodev_theme/` source), so :8090 is now a clean prod copy. NB: `docker exec ... rm /var/www/...` needs `MSYS_NO_PATHCONV=1` on Git-Bash or the path is mangled and the rm silently no-ops (gotcha `wpenv-windows-gitbash-path-mangling`).
- Drive via `docker exec <cli> wp eval-file ...` (cyrillic/quoting breaks inline `wp eval` — always eval-file). Do NOT run `do_action('admin_init')` in wp-cli (WC OrderAttributionController fatals). All rig traps: gotcha `wp-safe-remote-request-local-rig`.

### Docker inventory — DO NOT blindly prune (s28)

- **`wordpress-test` stack** (`wordpress-test` + `wp-mysql` + `wp-phpmyadmin`, volume `wordpress-test_db_data`, ~`:8080`) is the operator's **production-plugins test instance — ALL real plugins in one env** (intentional single instance, to test plugin↔plugin compatibility). **NEVER delete it or its volume, even when its containers are `Exited`.**
- Because that volume is unattached while the stack is down, **never run `docker volume prune` / `docker system prune --volumes` here** — it would wipe `wordpress-test_db_data`. Clean docker only surgically: `docker builder prune`, `docker image prune` (dangling), and explicitly-identified orphans.
- Project wp-env = `de59f74e…` (dev `:8973` + tests `:8974`); issuer = `c8ec47a5…` (`:8090`). Both KEEP.

## Infrastructure Reference

- **Version:** `Woodev_Plugin::VERSION` (in `woodev/class-plugin.php`) = 2.0.1 (unreleased).
- **PHP target:** 8.1 · **WP min:** 6.6 · **WC min:** 7.0
- **Tests:** Brain Monkey (unit) + WP Test Library (integration). `composer check` = phpcs + phpstan L3 + unit.
- **CI:** GitHub Actions. **Merge PRs:** `gh pr merge <N> --squash --delete-branch` only after confirmed-green CI; never `gh pr merge --auto`.
