# Current State — Woodev Plugin Framework

> Lean state doc: phase status, open bugs, next actions. **Full session history → `SESSION-LOG.md`** (newest on top). Program-level status → `platform-v2-program-tracker.md`.
> Last updated: 2026-07-05 (s40 — **CONDITIONAL/DEPENDENT FIELDS (show_if) SHIPPED** (PR #98 `32b58ff`): per-field `show_if` (array|callback), WP_Query-flat grammar `=`/`!=`/`in`/`not_in`, mirrored PHP↔JS evaluator, server `filter_visible_values` (posted-else-stored, order-independent), hidden field never blocks Save + not persisted, both surfaces. subagent-driven + two-stage review + Codex critic + **my browser e2e on `:8888`** (all 4 cases live). ADR-008. **Next = SP-4 (DaData seam).** See "Last session context".) · prior: session 39 — **SP-3 SHIPPED** (PR #96 `9ad9b5d`): field validation — `required` + email/url/tel/number(min/max), two-tier (client UX gate blocks Save/«Продолжить», server authoritative per-field error map), both surfaces. Subagent-driven + Codex critic + **my browser e2e self-verify on `:8888`** (Playwright; stars/blur-first/live-clear/Save-block/happy-path all confirmed). 910 unit / 61 integration / 0 risky, every CI job CLEAN. **Next = SP-4 (DaData seam).**).

## Last session context (≤3 lines)

- **s40 (CONDITIONAL FIELDS SHIPPED PR #98 `32b58ff`, 05.07.2026):** `show_if` conditional visibility across the Settings API + both React surfaces. Per-field `show_if` arg = a conditions **array** or a **callback** `fn(field_id):array` (DRY; returns data → mirror-safe). WP_Query-style flat grammar: `relation` (AND/OR) + `{setting, operator, value}`, operators `=`/`!=`/`in`/`not_in`, no nesting. Shared **mirrored PHP↔JS evaluator** (`Woodev_Setting::evaluate_conditions()` ↔ `evaluateConditions()`): string compare, bool→`'1'`/`''`, empty/non-scalar/unregistered controller→`''`, unknown op→fail-closed. Server `Woodev_Abstract_Settings::filter_visible_values()` strips hidden fields at the top of BOTH REST save paths (posted-else-stored, **order-independent two-pass**) → a hidden required field never blocks Save and isn't persisted. Client filters hidden in render + save-gate (both surfaces). Individual fields only; `>`/`<`/`empty`/`contains`/nesting/section-hiding → FUTURE-BACKLOG; contract in **ADR-008** (incl. `show_if` = UX not authz; no transitive hiding). 10-task subagent-driven + two-stage review (caught a REST-save crash on unregistered controller + a T5 mock regression) + **Codex critic + re-critic** (order-dependence, `||`→`??` fail-closed parity, non-scalar-target guard). **Browser e2e by me on `:8888`**: mode=live→api_key+`*`; mode=test→api_key hidden + Save persists; calc_type fixed/dynamic→rate/formula swap (callback+not_in) — screenshot sent. 942 unit / phpcs 186 / both bundles / **17 CI jobs pass + CLEAN** (one WP 6.6 npm-ECONNRESET flake → rerun green). `@since 2.0.2`. **🎯 NEXT = SP-4 (DaData seam).** Spec/plan `docs-internal/specs|plans/2026-07-05-conditional-fields-*`.

- **s39-polish (SP-3 POLISH SHIPPED PR #97 `1ea2be9`, 01.07.2026):** operator-requested follow-up after rig-testing SP-3. (1) **`placeholder`** support on fields (`register_control(['placeholder'=>…])` → control → schema → text inputs). (2) **per-field `validate` callback** (`register_setting(['validate'=>fn($v):bool,'validate_message'=>…])`): plugin-supplied PHP callable OVERRIDES the default format/type/enum check (server-authoritative; required still applies; runs only on non-empty; strict `true ===` fail-closed); `Field_Schema` emits `server_validated` so the client skips its (un-mirrorable) format check and relies on the atomic server error contract — lets a plugin enforce e.g. RU 11-digit while the framework's tel default stays loose ≥5. (3) **scroll-to-first-error + snackbar/banner** on blocked Save/«Продолжить», both surfaces, with a **generation counter** so the scroll re-fires on every blocked reveal (repeat Save / server-400-after-client-pass). Fixture «Карьер» demos all three. 918 unit / 61 integration / 0 risky, phpcs clean, 5 bundles, every CI job CLEAN; per-task spec+code-quality reviews + Codex critic (MED `(bool)`→`true===` fail-closed, fixed). **Browser e2e by me on `:8888`** (Playwright): placeholders render; `+79009` on support_phone passes client then server-rejects with the custom 11-digit message mapped to the field; snackbar shows — screenshot sent. **🎯 NEXT (operator's explicit ask): conditional/dependent fields** (show field B when A=X) — its own brainstorm→spec→subagent-driven impl; key design = skip hidden fields in BOTH client + server validation. Then SP-4 (DaData seam). `@since 2.0.2`, VERSION unchanged.

- **s39 (SP-3 SHIPPED PR #96 `9ad9b5d`):** `required`+star, email/url/tel/number(min/max) validation across Settings API + settings page + setup wizard. New: `tel`+`url` control types, `required` flag, unified `Woodev_Setting::get_validation_error()` (format from **controlType**), `Woodev_Abstract_Settings::validate_values()` + **atomic REST** two-pass (`{status:400, errors:{id:msg}}`, persist-nothing-on-error), JS `validate.js` faithful mirror, `ControlField` blur-first→live-clear + `serverError` precedence, `FieldRow` `<abbr>` star + `aria-live` error, settings+wizard Save/«Продолжить» gates («Пропустить» bypasses). **13-task subagent-driven + two-stage review** caught ~10 real bugs (dead-code, required-multi gap, `onTabChange` dropping client errors, wizard skip/stepper race). **Codex GPT-5.5 critic:** HIGH `is_email(array)`→500 crafted-payload guard; MED `['']` multiselect-required bypass — both fixed+re-critic'd CLEAN. **Latent framework bug fixed:** `is_email(null)`/`strpos(null)` PHP 8.1 deprecation when a format-typed setting has no default → guarded validators + `set_default(null)` short-circuit → gotcha `format-validator-null-strlen-deprecation`. **Operator's new hard rule honored:** self-verified e2e in the browser (Playwright on `:8888`) BEFORE merge — screenshot sent. 910 unit / 61 integration / 0 risky / phpcs 186/186 / 5 bundles build / every CI job CLEAN. `@since 2.0.2`, VERSION unchanged. Spec/plan: `docs-internal/specs|plans/2026-06-30-sp3-field-validation-*`.

- **s38 (SP-2 SHIPPED PR #94 `79a9d67`):** Implemented the full 14-task SP-2 plan subagent-driven (fresh agent per task, spec-compliance verified per diff). **Masking:** `sensitive` + `constant_name` on `Woodev_Setting` (constant precedence in `get_value`, skip-write in handler), `Field_Schema` emits `value=''`+`is_set`/`constant_managed` (secret never reaches the browser). **Auth contract:** `Settings_Section` `is_connection`+`action_label` (1..N blocks, 0 fields = handshake), seam interfaces `Woodev_Settings_Connection_Test`/`_Connection_Status` + `Woodev_Connection_Result` VO routed by `connection_id`, `POST woodev/v1/settings/{provider}/connection/{id}/test` with server-side stored-secret merge. **React:** masked `ControlField` + self-contained connection card + «Карьер» fixture (credential + handshake blocks). **Codex GPT-5.5 critic** (tight ~11KB inline bundle — a 27KB bundle HUNG 15min, cancel+resplit; ≤~12KB is the working size): HIGH (constant-undefined mask leak) + MEDIUM (connection-test merge fell back to stored for ANY empty field, not just secrets) — both fixed + re-critic'd RESOLVED → gotcha `mask-constant-backed-field-even-when-constant-undefined`. **Operator rig fix-loop:** button-disable until fields satisfied (handshake always-enabled, saved/constant secret counts as filled); **result-leak across sub-tabs** (missing React `key` on SectionView → state bleed → gotcha `react-missing-key-state-bleed-across-tabs`) + clear-result-on-edit; masked field «eye only when typing» + replace-by-typing (**dropped the broken «Очистить»** — wipe-secret/disconnect → FUTURE-BACKLOG); `.components-notice` margin. 886 unit + 4 connection integration green, phpcs clean, class-map regen, every CI job pass+CLEAN. `@since 2.0.2`, VERSION unchanged. **SP-3 decision #1 LOCKED:** live inline validation for email/url/tel/number = **blur-first → live-clear-on-input once errored**; `required`+star on blur+Save; client UX + server authoritative gate. Rest of the validation model → SP-3 spec (s39).

- **s37 (UK-2 SHIPPED PR #93 `60c622e` + SP-2 spec/plan, NO code):** **UK-2** — Plugins + Licenses pages onto the shared UI-kit (`@use tokens`; plugins: legacy cyan `rgba(0,201,253)`/`#00c9fd` → `$accent`, red `#b32d2e` → `$error`; licenses: WP-blue `#2271b1` + info `#72aee6` → cyan, status colors → tokens). Pure token unification, no layout change; build clean, grep-verified no source colors leak, every CI job CLEAN, operator rig-approved. **Only the wizard (UK-3/UK-4) remains un-kitted.** **SP-2** (operator chose over UK-CFR) — brainstorm locked 4 decisions (scope = secrets **+ auth contract**; test = **seam+plugin callback**; connected = **on-demand, framework stateless**; **no scheme presets** — "how to auth is domain"). Real-carrier-grounded: **1..N connection blocks** per provider, block = `Settings_Section` `is_connection` (free-form fields, or **0 fields = handshake**), masking (`sensitive`) orthogonal to auth. Spec `specs/2026-06-29-sp2-secrets-auth-design.md` (`eb55e02`) + 14-task TDD plan `plans/2026-06-29-sp2-secrets-auth-plan.md` (`4e7f431`). **s38 = execute the plan (subagent-driven + Codex GPT-5.5 critic); visual = operator rig-approval before merge.**

- **s36 polish (DONE — PR #91 `73130dc` + #92 `3ee4eed`, both operator-approved):** After UK-1, operator rig-reviewed the kit + settings in iterative batches. **#91 (UI-kit, 21 fixes across 3 batches → operator 10/10):** folder-tab/subtab spacing+clearfix; fixed-width tooltip; input radius+shadow; **ComboboxControl/FormTokenField replaced by a new WC-style `src/components/select-field.js`** (trigger button + search popover + checks, single & multi — the operator wanted the WC SelectControl UX, not inline combobox); overlay dropdowns (no page jump); range slider stretches with value+suffix pinned right; color = 1:1 square; number max-width 150px; uniform 40px control height; password show/hide eye; toggle meta padding. **#92 (settings page):** all field types demoed (fixture gained a «Прочее» section: email/password/color/date); first-field top spacing; toggle aligned to grid width + flush-left; **console clean** (SVG camelCase, `__next40pxDefaultSize` everywhere — drops 36px warning); **Save disabled until a change**; **native WP snackbar** on save (`@wordpress/notices` + `SnackbarList`) alongside the inline notice; **section descriptions** (`Settings_Section::create(...,$description)`, rendered under the sub-tab). All rig-verified `:8888`; 868 unit + integration green; every CI job CLEAN. **Extensibility (operator Q — custom fields/sections, not just tables) → deferred to FUTURE-BACKLOG `UK-CFR`** (two levels: (a) `woodev.settings.controlRenderer` hook for any custom field; (b) custom whole section for sub-CRUD tables like СДЭК упаковки / Яндекс склады — design both together against a real consumer).
- **s36 (DONE — UK-1 SHIPPED, PR #90 `4463f11`):** Built the shared admin **UI-kit** (research → brainstorm → spec → plan → impl, all this session). **Foundation:** `src/components/tokens.scss` (canonical accent `#06aedd`) + `_wp-recolor.scss` + neutral `_field.scss`/`field-row.js` + `control-field` made neutral (shared by settings+wizard via parent-context layout) + **`ComboboxControl`** (searchable) replacing the deleted custom `dropdown.js` + **`TabsNav`** (folder provider tabs + horizontal section sub-tabs + query-param deep-link). **Settings rebuilt** on the kit (full-width Card, one-section-at-a-time). **Dev-gated component gallery** `Woodev → UI Kit` (`WOODEV_UI_KIT_GALLERY` const/filter, TDD'd). **min WP 6.3→6.6** (deleted `babel.config.js`, automatic JSX runtime; license/plugins bundles now use react-jsx-runtime). plugins cyan unified; license stays WP-blue (UK-3). **All 9 operator UI points rig-verified on `:8888`** (folder tabs, deep-link, portal tooltip, no dividers, searchable select, sub-tabs, real range slider — the «broken dot» was a missing recolor, now fixed, all field types in the gallery); wizard unregressed; settings save persists (mode='live' key). **868 unit + 56 integration** green (incl. new `UiKitGalleryPageTest`), phpcs clean, every CI job pass + CLEAN. **GPT-5.5 (Codex) inline critic** (companion auth works but built-in review still hits the sandbox shell wall → inline-bundle is the working path) → 5 findings: CRITICAL false (class-map autoloader), HIGH non-issue (verified), MEDIUM+2×LOW fixed + re-critic'd (CONFIRMED). Decisions doc `docs-internal/specs/2026-06-27-ui-kit-design.md`, plan `…/plans/2026-06-27-uk1-ui-kit-foundation-plan.md`, research `…/research/2026-06-27-ui-kit-component-inventory.md`. VERSION unchanged (`@since 2.0.2`).

- **s35 (DONE — menu fix SHIPPED; UI direction pivot; SP-2 deferred):** Three threads. **(1) SP-1 rig-verified** on `:8888`: settings React UI works end-to-end (render, text+select save via `woodev/v1/settings`, **per-field dirty-tracking**, DB persistence, success notice). **(2) Release-blocker found+fixed → PR #88 `e9b9235` MERGED:** the top-level «Woodev» admin menu (parent of Licenses/Плагины/Настройки) was MISSING — reachable only by direct URL. Root cause (rig-confirmed): `Woodev_Plugin::load_admin_pages()` gated `instance()` on `! class_exists('Woodev_Admin_Pages')`, but that class is in `class-map.php` → the s27 runtime autoloader resolves it → `class_exists()` always true → menu never registered. **Hits every real v2 plugin.** Fixed via explicit static `$admin_pages_initialized` flag (TDD: `tests/integration/AdminMenuTest.php` RED→GREEN). Rig-verified: all 3 submenus appear + route. Gotcha `classmap-autoload-breaks-class-exists-once-guard`. **(3) Settings UI polish → PR #89 (DRAFT, NOT merged, SUPERSEDED):** operator reviewed the autonomous WC-settings polish (card panel, two-column rows, brand cyan) → "намного лучше, но не то". **Verdict: the 4 admin React surfaces (settings/plugins/licenses/wizard) each have their own scripts + divergent styles (license=WP-blue `#2271b1`, plugins=red `#b32d2e`, wizard+settings=cyan `#06aedd`; NO shared token partial) → build a shared UI-kit.** Operator's 9-point settings-UI review + the UI-kit decision recorded in `docs-internal/next-session-prompt.md`. **SP-2 (auth+secrets) DEFERRED until the UI-kit lands.** Polish branch `polish/settings-page-ui` (PR #89 draft) kept as reference (incl. a rich all-types demo fixture commit). 868 unit / 53 integration green throughout.
- **s34 (DONE — SHIPPED, PR #87 `39d31a6`):** SP-1 settings page — first shipping-module sub-project. `writing-plans` (10 TDD increments, `docs-internal/plans/2026-06-26-sp1-settings-page-plan.md`) → **plan reviewed by 2 independent critics before code** (7 fixes incl. HIGH `Woodev_REST_API_Settings` name-collision → renamed `_Page`, save-scope to declared ids, import re-point) → implemented **directly worker+inline-bundle-critics** (NOT the autodev-loop: its codex critic is non-functional on this Windows box — `invoke-critic.ps1` shell-spawns, dies on `CreateProcessAsUserW failed:5`; `.autodev/` also still pointed at the done S2 goal). New `Woodev\Framework\Settings\*` (`Settings_Page_Registry` singleton + `Settings_Provider`/`Settings_Section`/`Field_Schema` + seam `Woodev_Plugin::get_settings_providers()`), global `Woodev_REST_API_Settings_Page` (`GET/POST woodev/v1/settings`), React `src/settings-page/` + shared controls extracted to `src/components/`, fixture «Карьер». **Post-impl reviewed by 2 more critics + re-critic'd the fixes** (registry test-isolation, React cross-tab/stale-edit, +multi-key/multi-carrier/REST coverage). CI caught 2 version-specific integration-test reds (WC-deprecation from `do_action('admin_menu')`; `$submenu` accumulation) → fixed → **every job green + CLEAN** → squash-merge. **868 unit** + integration, phpcs/class-map/assets-parity clean. New gotcha `integration-test-global-admin-hooks-output-and-submenu-accumulation`. **⚠️ React page itself NOT rig-verified yet** (server-side + structure CI-green; operator to rig-test `:8888`). `@since 2.0.2`, VERSION unchanged.
- **s33 part-2 (DONE — SP-1 brainstorm + design spec, NO code; superseded by s34 ship):** First shipping-module sub-project. Interactive brainstorm (skill `brainstorming`) grounded on the real Settings-API + wizard code → **5 decisions LOCKED**: (1) **provider = a `Woodev_Abstract_Settings` handler** (own id→option namespace→tab→legacy key→migration); plugin contributes 1..N providers (multi-carrier = multiple tabs); thin `Settings_Page_Registry` aggregates. (2) **Storage = native per-option `woodev_{id}_{setting_id}` kept** (no runtime adapter); legacy single-array key = source of a **one-time `Woodev_Lifecycle` migration** written per-plugin at the Phase-E pilot, NOT SP-1. (3) **Registry + one fixture reference provider, NO DaData** (§9 = only the service seam; DaData = SP-4). (4) **One aggregated app + one controller `woodev/v1/settings`** routed by `{provider_id}` (not per-provider). (5) **Capability**: neutral default `manage_options`; **WC-dependent plugin → default `manage_woocommerce`**; provider override wins; parent-menu visibility reconciled in the plan. Spec: `docs-internal/specs/2026-06-26-sp1-settings-page-design.md`. **Next = `writing-plans` for SP-1, then implement via autodev-loop** (worker+critic, re-critic own fixes, independent review pre-merge).
- **s33 part-1 (DONE — SHIPPED, PR #86 `b7c738e`):** autodev-loop tooling hardening vs a "Loop Engineering" framework (Osmani/Cherny, from an operator video) + independent Codex GPT-5.5 gap-pass → **10 fixes**. Headlines: gate guard coverage per contract-VALUE not per-zone; cheap rubber-stamp critic tier removed (every non-empty diff hits the real critic; spark = cost lever); **dirty-file fence** with a content-**fingerprint** baseline; `DRIFT` escalates; 429 **backoff**; `-GuardBootstrap` removed; **branch preflight**; bounded worker↔critic retry; task schema (`success_commands`/`forbidden_paths`/`max_rounds`) consumed. **Codex re-critic'd TWICE** — all High/Med closed. conductor **8** / gate **5** / scheduler self-tests PASS; `composer check` green (842 unit). Gotcha `autodev-loop-gate-fence-pitfalls`. Tooling-only — no framework/shipping code. (Merge note: also fixed a pre-existing Markdown-Lint red on the tracked `SHIPPING-PLANS.md` — a missing `>` blockquote prefix; main was red on it independently.)
- **s32 (DONE — brainstorm/decisions, NO code):** Operator-led interactive brainstorm over `SHIPPING-PLANS.md` — **all 14 §20 gaps CLOSED** + program decomposed into 11 sub-projects + pilot-migration. Authoritative: `docs-internal/specs/2026-06-25-shipping-module-decisions.md`; draft fully resolved (gaps → ✅ РЕШЕНО). Key calls: §15 settings = `Woodev > Настройки` neutral React slot, 1 tab=1 provider; §8 fields = one core + 2 adapters (classic first, blocks fast-follow); §17 **warehouses dropped (YAGNI)**; §18 Pochta-GUID = domain legacy (misread corrected); §19 per-plugin Lifecycle migration; framework=mechanism+hooks, domain=specifics. **Next = SP-1 settings slot** (spec→plan→impl, one SP at a time). No framework code changed.
- **s31 (DONE — SHIPPED, operator-approved):** Setup Wizard UI redesign — modern WC-onboarding look rebuilt on `@wordpress/components`, **muted brand cyan `#06aedd`**, centered card, progress-line+dot clickable stepper, mandatory terminal finish, step descriptions + field anatomy (label/tooltip/description), **all control types** (text, custom dropdown, radio-in-group, number, range-slider, WC-pill toggle, richtext, multiselect). Settings-API gained `toggle`/`richtext`/`multiselect` control types + `min`/`max`/`step`/`tooltip` + `update_value` accepts enum **keys** + per-element is_multi; `register_setting` reorder (`set_is_multi` before `set_default`); wizard PHP emits controlType/description/tooltip/min/max/step, synthetic terminal `finish` step, `get_finish_actions()` cards + conditional `get_finish_secondary_actions()`; CSS versioned by `filemtime` (bust SCSS-only rebuilds). Full React rewrite `src/setup-wizard/`. **830 unit** (0 fail, 66 skipped), phpcs clean, PHPStan L3 clean, build byte-identical. Operator reviewed 3 fix-batches on rig `:8888` → **approved**. Demo fixture reverted to minimal before merge. PR #84 (`3aa8ab2`). **Follow-up PR #85 (`6e8b92e`) — Codex GPT-5.5 critic hardening:** independent critic (run via inline-bundle, `codex-shell-sandbox-broken-windows`) found real bugs CI+rig missed → fixed richtext XSS (`wp_kses_post`), enum validation bypass (key-or-value, incl. zero-based int keys), number-string coercion, `on_save` field-scoping, schema `is_multi`; re-critic'd twice → CONFIRMED-ALL-RESOLVED. **842 unit**. Deferred to backlog: forward-nav-to-finish, completion-failure UI, sensitive bootstrap values. Spec/plan: `docs-internal/specs|plans/2026-06-23-setup-wizard-ui-*`.

- **s30 (SUPERSEDED by s31, now SHIPPED):** OB-10 wizard UI work — full-screen render + first WC-React redesign attempt (Woo-purple, numbered stepper); operator rejected the design ("лучше, но не то"); reworked in s31 and merged. Original s30 detail kept for history: Verified on rig `:8888` → shipped wizard rendered inside admin-chrome + bare. Built **full-screen render** (admin_init interception → standalone HTML doc → exit) + **modern WC-onboarding React redesign** (header, numbered stepper, Card, finish; Woo-purple `#7f54b3`; `@wordpress/components` controls). Rig-demo seeding on the test fixture (multi-step + settings handler text/select/toggle; **revert before PR**). E2E-verified in browser (0 console errors). **Operator: much better but design NOT final — significant rework next session** (add mandatory `finish` step; brand image; step + option descriptions; field tooltips; more control types incl. richtext/radio/number/number-range-as-slider; maximal mock). main functionally unchanged. wp-env `:8888` left running. Details: s30 SESSION-LOG + `next-session-prompt.md`.
- **s29 (DONE — SHIPPED, PR #80 `ce04700`):** Setup Wizard (OB-10) rewritten — neutral React-driven, opt-in `Woodev\Framework\Setup\Setup_Wizard` (+ thin `Woocommerce_Setup_Wizard`) replacing the legacy SkyVerge fork (deleted base + its payment-gateway subclass; zero live consumers). Declarative PHP steps over the existing neutral Settings API; `Woodev_REST_API_Setup` on **woodev/v1**; opt-in via `build_setup_wizard_handler()` seam (null default); install→one-shot redirect + notice; React shell `src/setup-wizard/`. **782 unit**, full CI green incl. **Integration WP 6.4/6.6/latest** + PHPStan. **Independent review DONE** (3 agents, follow-up same day) → fixes shipped **PR #81** (real multi-plugin bug: `Woodev_REST_V1_Registrar` dedup-ed stateful controllers by class → 2nd wizard plugin's routes dropped; now per-plugin key + test; + `\Throwable` safety in REST; notice cap-guard; `skip()` finally) + **PR #82** hotfix (PHPStan `@return` on `complete()`). main = `6afd6dd`. ⚠️ **React UI still NOT browser-verified** (operator to rig-test tonight). ⚠️ Incident: #81 briefly merged on a red PHPStan Lint (my `&&`-chain didn't stop on UNSTABLE; main has no required-check gate) — caught + hotfixed in ~2 min; lesson recorded. Spec/plan: `docs-internal/specs|plans/2026-06-22-setup-wizard*`.
- **s28 part-2 (cleanup, no framework code):** dismantled the `:8888` consumer rig stand (stand plugin + s26 install stubs removed from `:8888`/`:8889`; `.wp-env-stand/`/`.rig-stubs/` deleted), removed the rig mu-plugin from issuer `:8090`, reclaimed ~7.3 GB docker. **`wordpress-test` stack = operator's prod-plugins test instance — never delete; never `docker volume prune` here** (see "Docker inventory" below). **Next session (s29) = OB-10 Setup Wizard — START WITH BRAINSTORM** (see `next-session-prompt.md`).
- **s28 (DONE — SHIPPED, PR #79 `f96e9ce`):** Competitor Notification module rebuilt as a reusable v2 framework module (PSR-4 `Woodev\Framework\Competitor\`, `@since 2.0.2`, version NOT bumped). Platform-neutral `Competitor_Notification_Handler` engine + declarative `Competitor_Rule` VO + pluggable renderers (`WC_Admin_Notes_Renderer` selected by `class_exists(Note::class)` — the gotcha-correct gate; `Admin_Notice_Renderer` fallback). Opt-in via `Woodev_Plugin::get_competitor_notification_handler()` (null default) on `current_screen`. Smart recommend link (connected+owned → extensions page via cached `woodev_account_purchases` transient; else public URL). Codex review (3 HIGH/3 MED/1 LOW) all fixed + re-critic'd (all sound). **760 unit** (+31), phpcs clean. PHPStan passed on **Linux CI** (local Windows segfault was environmental → new gotcha `phpstan-windows-parallel-worker-segfault`). Spec/plan: `docs-internal/specs|plans/2026-06-21-competitor-notification*`. Out of scope: yandex subclass migration (at plugin rewrite), Setup Wizard (OB-10).
- **s27 (DONE — SHIPPED, PR #78 `1aa4ec4`):** Framework runtime autoloader (`Woodev_Framework_Autoloader`, hand-written spl_autoload, **no Composer in shipped plugins**) + generated `woodev/class-map.php` + `bin/generate-class-map.php`. **Plugin type now declared solely by `extends`** — `capabilities` array fully removed (loader-definition, resolver `load_early_capability_classes` deleted, bootstrap gate, 7 fixtures). New `Woodev_Loader` facade. Resolves PLANS §5 (bootstrap fate + type declaration) as **variant C** (progressive path to godaddy versioned-namespaces, deferred — spec §8). Godaddy fork studied (OB-5 DONE — findings spec §9). **729 unit**, phpstan L3 + phpcs clean. Codex + Claude independent review (no CRITICAL/HIGH blocker); 3 findings fixed + re-critic. Autonomous overnight. Spec/plan: `docs-internal/specs|plans/2026-06-21-plugin-type-autoloader*`.
- **✅ B-2 loader-protocol forward-tolerance — HANDLED (discussed + resolved with operator s27):** the resolver loads framework **classes from the highest registered copy for the whole fleet**, regardless of which copy wins the bootstrap rendezvous — so a newer plugin never breaks against an older rendezvous winner; the older plugin (vs newer framework) is the at-risk one, and the **existing `backwards_compatible` min-version guard** (`resolver:148-153`) deactivates-with-notice any plugin below the loaded copy's min — same as v1. Two standing rules (now written in `AGENT-RULES.md` Rule 3): every loader definition MUST set `version` + `backwards_compatible`; the registration contract is additive-only from v2.0.0. The removed `capabilities` protocol was never released → cannot break any deployed plugin. See SESSION-LOG s27 + gotcha `framework-classmap-autoload-vendored-boot`.
- **s27 part-2 (design only, NO code):** **Competitor Notification module** brainstormed with operator → **spec ready** `docs-internal/specs/2026-06-21-competitor-notification-design.md` (5 decisions: declarative rules with `mode` recommend|conflict; neutral engine + WC-Notes/admin-notice renderers; smart recommend link-target via account ecosystem #8 with degradation; framework default i18n templates + per-plugin mapping; respect-dismissal + auto-delete). v1 raw ref: `woocommerce-yandex-delivery/woodev/handlers/competitor-notification.php`. **Implementation = s28** (start with `writing-plans`; see next-session-prompt). Setup Wizard parked as **OB-10** (separate brainstorm later).
- **s26 part-2 (DONE — SHIPPED, PR #77 `086585c`):** #8 install purchased plugin from connector. Rig e2e PASSED + operator browser-verified; Codex no CRITICAL/HIGH. **Connector deployed to prod woodev.ru.** Version NOT bumped (still 2.0.1 unreleased, `@since 2.0.2`).
- **s26 part-2 (build detail):** #8 install purchased plugin from connector. Framework: `POST woodev/v1/account/install` (cap `install_plugins`+nonce) → `Woodev_Account_Installer` (SSRF host-pin guard + `Plugin_Upgrader`, **no activation**) + React «Установить» button (idle/installing/done/error) in card + «Мои покупки». Connector (woodev_theme `d375d6d`): `GET /download/{id}` (HMAC + ownership) → **`edd_get_download_file_url`** purchase link (order-bound, not domain-bound — gotcha `edd-sl-package-download-domain-bound`); signed `woodev_install` marker bypasses per-file limit on that path only; account-scoped rate-limit. **725 unit** + 52 connector unit, phpcs/phpstan/build-parity green. Codex: no CRITICAL/HIGH; MEDIUM (atomic rate-limit) + LOW (https pin) **FIXED + re-critic'd**. Polish in PR: full-card install **spinner overlay** (`ab8fe38`) + **filterable request timeout** (install `/download` 30s, `284bdd0`). **Rig e2e PASSED** — server-side install smoke + operator browser-verified (install from catalog & «Мои покупки», inactive, error on file-less product).
- **PENDING (operator):** deploy connector to **prod woodev.ru**; merge PR #77 (`--squash --delete-branch`, not `--auto`); resync main. Rig left wired for re-tests (issuer mu-plugin `zz-rig-host-rewrite.php`, 3 stub products 36/23/26, re-seeded consumer). Issuer :8090 brought back via `npx wp-env start` (Bash + `MSYS_NO_PATHCONV=1`; volumes survived).
- **s25 (DONE — SHIPPED, PR #75 `bbc09bb`):** #7 «Мои покупки» tab + «Куплено» badge. `Woodev_Account_Purchases` + `GET woodev/v1/account/purchases` signed proxy + React tab + cyan «Куплено» badge. Codex-reviewed, rig-verified.
- **Bonus fix (s25):** failed/denied connect bounced to the extensions page silently — added `Woodev_Account_Connection::render_connect_notice()` on `admin_notices` + clearer denial message.
- **Flagged, NOT fixed (operator decision pending):** catalog proxy uses default 5s `wp_safe_remote_get` timeout; issuer products endpoint ~8.6s → cold-cache catalog fails (gotcha `extensions-catalog-fetch-5s-timeout`). One-line fix available. v2.0.1 still unreleased → `@since 2.0.2`.

## Program status (high level)

| Stage | Status | Notes |
|---|---|---|
| S0 Platform Split | ✅ DONE | tag `platform-v2-split-done`; base platform-neutral, resolver minimal, clean-break Phase 3 shims deleted |
| S1 Shipping | ✅ DONE | PR #20; PSR-4 module; rate/packing seam + conformance audit (s2–s4) |
| S2 Box-packer | ✅ DONE | PR #21/#22; woven into rate-calc single-seam template |
| S3 Licensing | ✅ DONE | need-license (PR #25) → React UI (PR #31) → webhooks + Ed25519 signing (PR #35) |
| Remote-deactivation UX | ✅ DONE | s10–s12; command cycle proven live (push prod + pull rig); B-13/14/15 resolved |
| S4 EDD / S5 React admin / S6 ecosystem | ⚪ deferred | post-v2.0 |

`composer check` green at s34: **868 unit tests** / 2430 assertions (66 skipped), integration green (incl. SP-1 `SettingsPageTest` + `SettingsPageRestTest`, `SetupWizardRestTest`). Keep green after each change. Note: PHPStan crashes locally on Windows (`-1073741819`, environmental — gotcha `phpstan-windows-parallel-worker-segfault`); Linux CI "Run PHPStan" is the authoritative gate.

## Phase Status (subsystems)

| Phase | Code | Browser-verified | Notes |
|-------|------|------------------|-------|
| Framework Core | ✅ | ✅ | Bootstrap, Plugin base, Lifecycle — stable |
| Payment Gateway | ✅ | ✅ | `class-payment-gateway.php`: **~3,542 lines** (whole tree ~13.8k); trait-extraction candidate |
| Shipping Method | ✅ | ✅ | PSR-4 namespaced |
| Licensing | ✅ | ✅ | EDD store integration; React license page on core `woodev/v1` REST |
| Settings API | ✅ | ✅ | Typed settings framework |
| Box Packer | ✅ | ✅ | Shipping box-packing algorithm |
| REST API | ✅ | ✅ | Plugin REST routes |
| PHPStan | ✅ | — | Level 3, **no baseline** (`phpstan-baseline.neon` removed; do not reintroduce) |
| Documentation | ✅ | — | Two-tier: `docs/` (GH Pages) + `docs-internal/` (AI agents) |

## P6 gate evidence — base is platform-neutral & not a god-object (reference)

- **Platform neutrality:** base `Woodev_Plugin` declares **zero** WooCommerce/HPOS-named methods; the last HPOS seam (`is_hpos_compatible()`) was removed. Late-safe WC hooks live in `Woodev\Framework\Woocommerce_Plugin::register_woocommerce_hooks()`; early `before_woocommerce_init` feature declarations are wired by the bootstrap from loader `supported_features` metadata. Enforced by `PlatformNeutralBaseHasNoWcMethodTest`, `PlatformNeutralRestApiTest`, `BootstrapRegistrationTest`.
- **Base size (2026-06-04):** `woodev/class-plugin.php` ~1,274 lines / 74 methods (56 public) after the P6 split-done follow-up.
- **Construction shape:** `__construct()` is an ordered list of `init_*_handler()`/`load_*` calls ending with `add_hooks()`; `add_hooks()` wires only base-owned hooks.

## Known Bugs / Open debt

- ✅ **[RESOLVED s21] License page Item 0** — bad/non-existent key no longer strands the user (PR #68). See SESSION-LOG s21.
- [⚠️] `class-payment-gateway.php` ~3,542 lines — trait-extraction candidate (grooming, s13; → `FUTURE-BACKLOG`).
- [ℹ️ OB-7 follow-up] «Плагины» still shows discontinued/coming-soon items (Беру.ру/GOODS) — `edd-api/v2` exposes no `_coming_soon`/`_product_icon`/rating; needs a woodev.ru-side API extension (s22 task #1). Framework normalizer already consumes them forward-compatibly.
- **All earlier release-blocker findings RESOLVED** (2026-06-01 audit, PHPStan masks, base-class leaks, eCheck/ACH removal, payment-gateway base-method regression, etc.) — see `SESSION-LOG.md` + git history. Not repeated here.

### Public-docs API staleness — DEFERRED (operator decision, s13)

- `docs/` (GH Pages) registration examples still teach the **v1 `register_plugin( '1.4.0', ... )` positional API**, which in v2 is a **tombstone** (quarantines the caller, never registers). The live API is `register_loader_definition([...])`. Examples also hardcode `'1.4.0'`/`VERSION='1.4.1'` instead of the `%%FRAMEWORK_VERSION%%` placeholder / `2.0.1`. Affected: `getting-started.md`, `core-framework.md`, `payment-gateway.md`, `shipping-method.md`, `README.md`.
- **Operator decision (s13): do NOT touch public docs yet** — he is currently the only consumer of the framework; the public docs get rewritten once everything is fully ready. Recorded so it is not mistaken for an oversight.

## Next Actions

- ✅ **s21 DONE — license Item 0 + OB-7 «Плагины» React redesign + polish (PRs #68/#69/#70):** see "Last session context" + `SESSION-LOG.md` s21. 665 unit, rig-verified, CI green. Spec/plan: `docs-internal/specs|plans/2026-06-18-plugins-page-ob7-redesign*`.
- ✅ **s22 DONE — OB-7 store-side (cross-project, woodev_theme) + plugins-page rating (PR #71):**
  1. **`woodev-core` edd-api (DONE, woodev_theme `8a32fda`):** `enrich_product()` (hooks `edd_api_products_product_v2`) now sends `info._product_icon` (theme `_product_icon` attachment-ID → URL) + `info._coming_soon` (bool incl. legacy `edd_coming_soon`). **rating was already present** (top-level, 0–100). Real keys found in `woodev-theme/inc/metaboxes.php`.
  2. **`woodev-account-connector` (DONE, woodev_theme, new plugin):** WC-Helper-style OAuth provider per spec §7. 6 endpoints + authorize screen + connections table + HMAC + EDD purchases. 31 tests, rig-verified, Codex-hardened (timestamp-freshness / atomic consume / same-origin). Driven by me + Codex critic (operator's choice). Deferred Low: rate-limit `/oauth/request_token` (woodev_theme FUTURE-BACKLOG).
  3. **Framework follow-up (DONE, PR #71):** `normalize_product()` surfaces a 0–5 `rating` + React card stars. **Forward-compat for live handshake stays gated** (`woodev_extensions_account_enabled` default false) until the framework **account client** (`Woodev_Account_Connection`, spec §7) is built — that is the open Phase-B item.
- ✅ **s23 DONE — catalog polish (OB-8, PR #72) + account-connection client SPEC.** See "Last session context" + SESSION-LOG s23. Spec: `docs-internal/specs/2026-06-19-account-connection-client-design.md`.
- ✅ **s24 DONE — account-connection client implemented + SHIPPED (PRs #73/#74):** `Woodev_Account_Connection` + connect/return handlers + REST disconnect + `AccountMenu` #6/#9 + `Установлен` badge #5 + OAuth `state` binding; connector authorize → front-end `parse_request` screen (WC_Auth-style) + branded `/login` + richer approval; flag `woodev_extensions_account_enabled` flipped default true after prod connector deploy. Rig-verified, Codex-reviewed. See SESSION-LOG s24.
- ✅ **s25 DONE — #7 «Мои покупки» + «Куплено» badge (PR #75 `bbc09bb`):** `Woodev_Account_Purchases` + `GET woodev/v1/account/purchases` signed proxy + React tab (cross-ref link by id) + cyan «Куплено» badge («Установлен» wins) + connect-notice fix. 706 unit, Codex-reviewed, rig-verified. See "Last session context" + SESSION-LOG s25.
- ✅ **s26 part 1 DONE — catalog fetch timeout fix (PR #76 `9d67f67`):** `Woodev_REST_API_Extensions::FETCH_TIMEOUT = 20` passed to both store fetches (default 5s was too short for the ~250KB enriched products → cold-cache `stale`). 707 unit, CI green. Gotcha `extensions-catalog-fetch-5s-timeout` marked fixed.
- 🎯 **s26 part 2 (PR #77 OPEN, do NOT auto-merge) — #8 install-from-connector DONE pending triage+e2e:** `Plugin_Upgrader` + connector `GET /download/{id}` using the order-bound `edd_get_download_file_url` purchase link (NOT the domain-bound SL `package_download` token — gotcha `edd-sl-package-download-domain-bound`). Codex: no CRITICAL/HIGH. **Morning:** triage 3 findings (MEDIUM atomic throttle, LOW https-pin, INFO) → apply + re-critic; rig e2e; operator deploys connector to prod; merge.
- ℹ️ **Rating-in-API (woodev_theme, deferred):** public edd-api omits `rating` despite reviews existing (`query_reviews()`/global-`$post` gap; repro inconclusive). Operator-skipped — revisit on the woodev_theme side if/when "Мои покупки" lands.
- ✅ **OB-3 COMPLETE** (s15 F11/F12/F13, s16 F2/F7+F5, s17 move, s18 F8/F9/F10, s19 F1/F3); only **F6** backoff deferred (endpoint-wide-key question).
- 📥 **Remaining backlog** (`FUTURE-BACKLOG.md` → "Operator backlog dump — s13"): OB-4 reusable-JS-php-based principle · ~~OB-5 godaddy fork study~~ **DONE s27** (findings in spec `2026-06-21-plugin-type-autoloader-design.md` §9; borrow candidates: `Block_Integration_Trait` High, `Enum_Trait`+pseudo-enums Med, `CanConvertToArrayTrait` Med) · OB-7 modernize Plugins page · OB-9 shipping nuances.
- **🎨 UI-kit program (s37 — UK-2 SHIPPED):** shared admin UI-kit for the 4 React surfaces. **✅ UK-0 research** + **✅ UK-1 foundation+settings (PR #90)** + **✅ polish (PR #91/#92)** + **✅ UK-2 Plugins + Licenses on the kit (PR #93 `60c622e`)** — token unification, Licenses left WP-blue (last palette outlier closed). Kit: tokens + recolor + neutral field layer + `field-row` + `select-field` + `tabs-nav` + dev gallery. **Remaining: UK-3/UK-4 wizard** (the only un-kitted surface; largest, full-screen; remove the dead `woodev-setup__field*` blocks superseded by `_field.scss`, drop duplicate `$wd-*` vars) — deferred (operator chose SP-2 next). **UK-CFR (settings extensibility, FUTURE-BACKLOG) — deferred to a later cycle against a real consumer.** Minor deferred: CI integration matrix still tests WP 6.4 though min is now 6.6 (PHP-only jobs pass; bump the floor when convenient); TabsNav empty-then-filled mount-URL edge (won't-fix).
- **🚧 BIG — Shipping module (s32 decomposition DONE):** all 14 operator gaps closed; program decomposed into **SP-1…SP-11 + pilot-migration** (Yandex→CDEK→Pochta) in dependency order. Authoritative map: **`docs-internal/specs/2026-06-25-shipping-module-decisions.md`**; resolved draft: `SHIPPING-PLANS.md`. **Process: one SP at a time** (spec→plan→impl→CI→merge), NOT all-specs-then-bulk. **✅ SP-1 «Страница настроек» (§15) SHIPPED s34 (PR #87 `39d31a6`)** — registry + `woodev/v1/settings` REST + React `Woodev > Настройки`; spec `docs-internal/specs/2026-06-26-sp1-settings-page-design.md`, plan `docs-internal/plans/2026-06-26-sp1-settings-page-plan.md`. SP-1 React page **rig-verified s35** (works; menu-blocker fixed PR #88). **✅ SP-2 «auth + секреты» SHIPPED s38 (PR #94 `79a9d67`):** `sensitive` masking + `constant_name` wp-config override + universal **connection-block auth contract** (1..N blocks/provider, seam+plugin-callback, `Woodev_Connection_Result` VO, `POST …/connection/{id}/test` with server-side secret merge) + React card. Spec `specs/2026-06-29-sp2-secrets-auth-design.md`, plan `plans/2026-06-29-sp2-secrets-auth-plan.md`. Operator rig-approved; Codex critic 2 findings fixed+re-critic'd. **✅ SP-3 «поля + валидация» SHIPPED s39 (PR #96 `9ad9b5d`):** `required`+звёздочка, email/url/tel/number(min/max), two-tier (client UX blocks Save/«Продолжить» + server authoritative atomic per-field error map), both surfaces. Source = `controlType` (+`tel`/`url` control types, `required` flag); `get_validation_error()`↔`validate.js` mirror; `validate_values()`+atomic REST; blur-first→live-clear. Browser-verified by me on `:8888`. Spec/plan `…/2026-06-30-sp3-field-validation-*`. **✅ Conditional/dependent fields (show_if) SHIPPED s40 (PR #98 `32b58ff`):** per-field `show_if` (array|callback), flat `=`/`!=`/`in`/`not_in`, mirrored PHP↔JS evaluator, `filter_visible_values` strips hidden at both REST save paths (hidden required never blocks Save), both surfaces; ADR-008; spec/plan `…/2026-07-05-conditional-fields-*`. **🎯 NEXT = SP-4 (DaData seam)** … SP-11 → пилот (Яндекс→СДЭК→Почта). Skeleton still **never validated by a real plugin** (s27 gaps: status-view 30% stub; no label/export abstraction; webhook not yandex-validated) — absorbed by the remaining SPs + pilot. Cross-cutting constraints (HPOS-safe meta, no `_n()`, class-map regen) in the decisions doc.
- **Big ones (operator-scheduled, not solo):** payment-gateway trait extraction (autodev-loop); the big review #4 — `array()`→`[]` (~797) + type declarations everywhere + `@since` sweep + enforce `Generic.Arrays.DisallowLongArraySyntax`. ~~B-2 loader-protocol forward-tolerance~~ **HANDLED s27** (highest-version classes always load regardless of rendezvous winner; `backwards_compatible` guard deactivates-with-notice too-old plugins; rules in AGENT-RULES Rule 3 — see "Last session context"). box-packer (s27 audit): minimal-virtual-box already replaced by a grid heuristic (PR #21, works for PLANS examples, not provably optimal); non-WC wrapper still deferred — closer to done than thought.

## 🔔 Cross-Project Reminder — Ecosystem Orchestration (dormant)

- **Trigger:** v2.0.0 shipped AND stable in production for several weeks. When it fires, surface it in the session-opening summary; do **NOT** auto-start; point the operator to the spec and read its "Prompt for the Future Agent" section first.
- **Spec:** `D:\Projects\woodev_theme\docs\superpowers\specs\2026-05-13-woodev-ecosystem-orchestration-spec.md`. Cross-ref: `FUTURE-BACKLOG.md` → "Cross-Project Initiatives" #7.

## Local rig

- **s28 — consumer stand DISMANTLED (operator request).** The s11/s12 PULL consumer stand on `:8888` was removed: deleted `.wp-env-stand/` (the `woodev-stand` plugin), the `woodev-stand` mapping in `.wp-env.override.json`, `.rig-stubs/` (s26 install stubs), and the `.gitignore` entry. The project wp-env (`.wp-env.json` fixtures) + `composer test:integration` are untouched and were never dependent on the stand. To drop the stand plugin from a running instance: `wp-env start --update`. Rig knowledge retained in gotcha `wp-safe-remote-request-local-rig` (historical).
- **Issuer `:8090` — KEPT, do NOT touch.** It is effectively a copy of prod (woodev_theme = local woodev.ru + EDD SL + deactivator, with test data); operator uses it independently. Container `c8ec47a5...-wordpress-1`. Authority pubkey `QSisoK0CDOmIOqGHvilMe+4mB/LMRFHf9hi6BxatfMk=`. The rig-only `zz-rig-host-rewrite.php` mu-plugin was **removed in s28** (from the container's `wp-content/mu-plugins/` and the `woodev_theme/` source), so :8090 is now a clean prod copy. NB: `docker exec ... rm /var/www/...` needs `MSYS_NO_PATHCONV=1` on Git-Bash or the path is mangled and the rm silently no-ops (gotcha `wpenv-windows-gitbash-path-mangling`).
- Drive via `docker exec <cli> wp eval-file ...` (cyrillic/quoting breaks inline `wp eval` — always eval-file). Do NOT run `do_action('admin_init')` in wp-cli (WC OrderAttributionController fatals). All rig traps: gotcha `wp-safe-remote-request-local-rig`.

### Docker inventory — DO NOT blindly prune (s28)

- **`wordpress-test` stack** (`wordpress-test` + `wp-mysql` + `wp-phpmyadmin`, volume `wordpress-test_db_data`, ~`:8080`) is the operator's **production-plugins test instance — ALL real plugins in one env** (intentional single instance, to test plugin↔plugin compatibility). **NEVER delete it or its volume, even when its containers are `Exited`.**
- Because that volume is unattached while the stack is down, **never run `docker volume prune` / `docker system prune --volumes` here** — it would wipe `wordpress-test_db_data`. Clean docker only surgically: `docker builder prune`, `docker image prune` (dangling), and explicitly-identified orphans.
- Project wp-env = `de59f74e…` (dev `:8888` + tests `:8889`); issuer = `c8ec47a5…` (`:8090`). Both KEEP.

## Infrastructure Reference

- **Version:** `Woodev_Plugin::VERSION` (in `woodev/class-plugin.php`) = 2.0.1 (unreleased).
- **PHP target:** 8.1 · **WP min:** 6.3 · **WC min:** 7.0
- **Tests:** Brain Monkey (unit) + WP Test Library (integration). `composer check` = phpcs + phpstan L3 + unit.
- **CI:** GitHub Actions. **Merge PRs:** `gh pr merge <N> --squash --delete-branch` only after confirmed-green CI; never `gh pr merge --auto`.
