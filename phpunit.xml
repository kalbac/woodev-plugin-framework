<?xml version="1.0" encoding="UTF-8"?>
<phpunit xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
		 xsi:noNamespaceSchemaLocation="https://schema.phpunit.de/9.6/phpunit.xsd"
		 bootstrap="tests/bootstrap.php"
		 colors="true"
		 executionOrder="depends,defects"
		 beStrictAboutOutputDuringTests="true"
		 beStrictAboutTodoAnnotatedTests="true"
		 failOnRisky="true"
		 failOnWarning="true">

	<!-- =====================================================
		 Test Suites
	====================================================== -->
	<testsuites>

		<!-- Юнит тесты: без WordPress, с Brain Monkey -->
		<testsuite name="Unit">
			<directory suffix="Test.php">./tests/unit</directory>
		</testsuite>

		<!-- Интеграционные тесты: внутри wp-env с реальным WordPress -->
		<testsuite name="Integration">
			<directory suffix="Test.php">./tests/integration</directory>
		</testsuite>

	</testsuites>

	<!-- =====================================================
		 Coverage: какой код измеряем
		 Указываем папку фреймворка ./woodev
	====================================================== -->
	<coverage>
		<include>
			<directory suffix=".php">./woodev</directory>
		</include>
		<exclude>
			<!-- Исключаем файлы которые не нужно покрывать тестами -->
			<directory>./woodev/assets</directory>
			<directory>./woodev/languages</directory>
		</exclude>
	</coverage>

	<!-- =====================================================
		 PHP INI настройки для тестов
	====================================================== -->
	<php>
		<ini name="display_errors" value="1"/>
		<ini name="error_reporting" value="-1"/>

		<!-- #632. The unit suite peaks around 126 MB and a typical local php.ini allows 128M.
			 While the run is GREEN that gap is invisible. On a RED run it is not: PHPUnit holds
			 the error objects, hits the ceiling, and dies with a fatal inside Patchwork instead
			 of printing the failures — so the symptom hides the cause. Measured in s102, where
			 the real cause was 268 errors from a WC() stub leaking process-wide and the first
			 thing the run showed was a fatal in someone else's library.
			 Invisible in CI, which is the point: setup-php sets memory_limit=-1 there.
			 Operator decision on #632. -->
		<ini name="memory_limit" value="512M"/>
	</php>

</phpunit>
